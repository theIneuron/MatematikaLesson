# 7 КЛАСС — ТОЧКА ВХОДА. Читать первым

Записано 2026-08-15. Любая задача по 7 классу начинается отсюда.

## ПОРЯДОК РАБОТЫ. Решение методиста 2026-08-21

**Сначала собираются ВСЕ уроки курса, замеры экранов — в конце.** Полный замер идёт
больше десяти минут на урок (пять размеров, три языка, около 3400 измерений), и
методист решил не ставить его в цикл каждого урока. Из цикла урока он убран.

В цикле урока остаётся то, что мгновенно или занимает минуту:
`check-grade7.mjs`, `grade7-lesson-audit.mjs`, `npm run build`, блиц и быстрый замер
(`GRADE7_FAST=1`, только ноутбук и два языка).

**НАПОМНИТЬ МЕТОДИСТУ О ПОЛНОМ ЗАМЕРЕ, КОГДА БУДЕТ СОБРАН ПОСЛЕДНИЙ УРОК КУРСА.**
Это его прямая просьба. Полный замер — единственное место, где видны телефонные
размеры: 2026-08-21 он нашёл в уроке 4 прозу, уезжающую вбок на 271 px (ru) и
353 px (en) на 360×690, а быстрый замер этого не видел.

**Долг: уроки, ждущие полного замера.** 4, 19–48. Полный замер пройден у уроков 1–18.
**КУРС СОБРАН ЦЕЛИКОМ 2026-08-21: 48 уроков из 48, все зелёные на `check-grade7`, аудите
и `npm run build`. Условие напоминания выполнено — полный замер пора ставить.** Отдельно
смотреть уроки 40–48: прибор `Figure` и `Plane` человеческими глазами не видел никто,
а прибор — единственное, чего статические проверки не покрывают в принципе.

**Долг практик: собраны, но не замерены и не прогнаны браузером.** Практики ВСЕХ 48 уроков
(480 заданий). По раскладке (grade7-practice-layout.mjs) собраны уроки 4 и 17–48; уроки
1–3 и 5–16 собраны раньше, до правила о раскладке. Браузерный прогон с ответами
(`grade7-practice-check.mjs`), замер высоты (`grade7-noscroll.mjs`), скриншоты и блиц
отложены до конца курса — решение методиста 2026-08-21. Что прогон в конце обязан
поймать: высоту задания на окне 615 (норма — не выше 363 px), разбор на каждый неверный
путь, три языка на каждом экране.

**Два долга слоя практик, найдены 2026-08-21, не закрыты.** Первый: фон практики белый
(`pq-fixroot` → `#fff`), а `AMALIYOT_GLOBAL_STANDART.md` требует `#fff7ed`. Второй:
варианты ответа не перемешиваются — в `kit.jsx` нет shuffle, и во всех 22 заданиях с
вариантами стоит `correct: 0`, то есть верный ответ всегда первый. Лечится в слое, файлы
заданий не меняются.

**Практики уроков 1–16 собраны не по раскладке** (`grade7-practice-layout.mjs`): механика
бралась из головы, использованы `TypeValue` и `MarkAll`, которых в пуле раскладки нет, а
`choice` встречается до трёх раз на урок вместо одного-двух и вне разогрева. Урок 17 —
первый, собранный по раскладке. Решение о переводе старых практик на раскладку методист
ещё не принял.

Два места, где риск переполнения выше среднего и это надо посмотреть в первую очередь:
прямоугольник 2×3 в уроках 27 и 31 (три колонки по 104 px плюс ярлык — на телефоне 360 это
почти вся ширина) и зоны из четырёх колонок в уроках 28 и 29.

**И третье, самое важное: прибор `Plane` глазами не видел никто.** Он проходит сборку,
приёмку и статическую проверку (все 14 классов есть в `STYLES`, геометрия в кадре, оси по
центру, масштаб равный), но цвета, толщины линий, читаемость подписей на телефоне и
попадание экрана в бюджет высоты машиной не проверяются. Один скриншот на один экран
закрывает это за минуту — методист решения по нему пока не дал.

---

**Эталон класса — урок 1 «Числовые выражения».** Он собран, проверен прогоном и
запушен. Новые уроки делаются под него.

**Блок Б1 закрыт полностью 2026-08-21.** Уроки 1, 2, 3, 5, 6 собраны 2026-08-16, урок 4
собран 2026-08-21 по решению методиста («урок 4 тоже надо сделать»). До этого он был
отложен: слова «тождество» в учебнике нет — проверено машинно в **обоих** изданиях,
`ayniyat` и «тождество» дают ноль вхождений на 192 страницах. Поэтому математика урока
взята из того, что учебник даёт: свойства действий (стр. 23) и упрощение выражений. Само
название `ayniyat` — **draft, требует валидации узбекским методистом математики**; если
методист его не подтвердит, слово убирается из четырёх мест урока (экраны 4, 6, 8, 14) и
заменяется на «bir xil qiymatli ifodalar», математика при этом не меняется.

**Блок Б7 ЗАКРЫТ 2026-08-21, и вместе с ним закрыт весь курс: уроки 40–48 собраны.**
Что стоит знать про них, помимо содержания:

- **Прибор `Figure` вскрыл дефект на первом же уроке:** углы считались по трём точкам даже
  тогда, когда `seg` задавал не треугольник, а открытую ломаную (луч и прямая в уроке 40).
  Теперь углы считаются только на ЗАМКНУТОМ треугольнике, а на открытом чертеже подписи
  даёт `notes`. Ни одна проверка этого не поймала бы: она печатала три числа, и все три
  были бы бессмысленны.
- **Флаг `guess` был холостым.** Он подписывал только строку суммы, а сумма углов — тема
  урока 44; в уроках 42 и 43 его поставили бы, и на экране не появилось бы ничего. Теперь
  он подписывает любое показанное измерение.
- **Долг урока 44 закрыт уроком 45.** Сумма углов в 44 честно названа закономерностью,
  добытой измерением; доказательство даёт экран переноса урока 45 — прямая через вершину,
  параллельная основанию. Это единственный экран переноса в курсе, который не бытовой.
- **Клик-задания проверены на единственность решения** (урок 47, `scratchpad/d47.mjs`):
  прибор сверяет ответ с одним `pick`, поэтому второй верный узел был бы отмечен как
  ошибка. Условие даётся двойным (равное расстояние И его величина), иначе задание нечестно.
- **Урок 46 не касается теоремы Пифагора** (8 класс): гипотенуза измеряется прибором,
  а хук построен на ловушке «сложить катеты».
- **Урок 48 — финал курса**, и `nextTopic` в нём указывает на 8 класс, а не на следующий урок.

**Блок Б7 начат 2026-08-21. Два решения методиста.**
- **Прибор блока — чертёж на КЛИКАХ, не на перетаскивании.** Эталон назначает на все семь
  ошибок блока прибор 5, «доказательство на чертеже», и требует движения вершины: у
  равнобедренного равенство сторон пропадает, сумма углов пересчитывается. Вершина
  переставляется кликом по узлу сетки — механика клика уже написана и проверена в `Plane`.
  Полноценное перетаскивание мышью вдвое дороже и даёт то же самое.
- **Урок 46 остаётся ОДНИМ** и берёт соотношения между сторонами и углами: именно её ошибка
  названа в эталоне («гипотенузу не считают наибольшей стороной»). Прямоугольные треугольники
  входят как случай, отдельной темой не разворачиваются.

**Важное ограничение эталона по Б7, оно меняет содержание уроков 40 и 41.** Требование
«измерение не доказательство» включается только с § 9 учебника. В уроках 40–41 измерение
отрезков и углов — сама тема, и линейка там инструмент, а не соблазн. Экран, объявляющий
измерение недопустимым раньше, противоречит учебнику. Поэтому пометка «предположение»
появляется на приборе с урока 42, а не раньше.

**Блок Б6 ЗАКРЫТ 2026-08-21:** уроки 33–39 собраны, все проходят `check-grade7`, аудит и
сборку. Линия блока: плоскость и пара координат (33) → функция как «одному x одно y» (34) →
график линейной функции и знак `k` (35) → два направления, построить и прочитать (36) →
`y = kx` проходит через начало (37) → система как пересечение двух прямых (38) → варианты
считаются умножением, а не сложением (39).

**Прибор блока написан: `Plane` в `tools.jsx`**, 203 строки вместо 868 у `grade8/plot.jsx`.
Из восьмого класса взяты четыре решения, а не код: кривая строится из функции `f`, тап
проверяется в математических координатах через долю от `rect`, оси со стрелками и подписями,
прибор показывает следствие и не называет ответа. Вид экрана — `plane` в конвейере.

**Две правки прибора, обе от проверок.**
- Подпись координат у ГОТОВЫХ точек выдавала ответ на задании «прочитай координаты».
  Теперь подписывается только точка ученика; готовым нужен явный флаг `labels`.
- Масштаб осей был разным (37 px по `x` против 21 по `y`), то есть `y = x` не шла под 45°.
  Равный масштаб теперь обеспечивает сам прибор: он берёт минимальный из двух и центрует
  поле. Данные урока на это влиять не могут.

**Окно подбирает урок под свою функцию.** В окне по умолчанию (`x` от −7 до 7, `y` от −4 до 4)
крутая прямая уходит за кадр быстро: `y = 2x + 1` видна на четырёх единицах из четырнадцати.
Это не дефект, а следствие равного масштаба, и записано в комментарии прибора.

**Блок Б6: два решения методиста по объёму тем.**
- **Урок 38 «Системы линейных уравнений» остаётся ОДНИМ.** В него входит графический
  способ и проверка подстановкой в оба уравнения — та ошибка, что названа в эталоне
  («нашли x, забыли y»; «точку пересечения берут на глаз»). Способы подстановки и
  сложения в курс не попадают. Это дыра в программе, как и с дробями в уроке 32.
- **Урок 39 берёт КОМБИНАТОРИКУ**, а работа с данными остаётся за рамками курса.
  Причина та же: в эталоне названа ошибка комбинаторики («складывают вместо умножения»),
  и закрывается она деревом вариантов.

**Прибор блока Б6 — прибор 4 эталона, «четыре окна функции».** Он назначен эталоном на
все семь ошибок блока, но не написан. Пишется на ядре 7 класса; из `grade8/plot.jsx`
берутся четыре РЕШЕНИЯ, а не код (подключать его нельзя — он завязан на ядро и палитру
8 класса): кривая строится из функции `f`, а не из списка точек; тап проверяется в
математических координатах с допуском в единицах оси, а не в пикселях; оси со стрелками,
подписями и делениями; прибор показывает следствие, ответа не называет.
Для урока 39 отдельная маленькая надстройка — дерево вариантов.

**Блок Б5 ЗАКРЫТ 2026-08-21:** уроки 25–32 собраны, все проходят `check-grade7`, аудит и
сборку. Один скелет на восемь уроков — формы экранов фиксированы, меняются данные; хук,
разграничение, граничный случай и ловушка в каждом уроке свои. Приборы только готовые:
прямоугольник площади (25–31), зоны (28, 29), лента множителей (32), подстановка и сборка.

Линия блока: квадрат суммы, две клетки `ab` (25) → разность квадратов, средние уничтожаются
(26) → куб, шесть клеток дают тройки (27) → выбор формулы по признаку, четвёртая зона
«формулы нет» (28) → формула наоборот: свернуть многочлен (29) → несколько действий в одной
записи, минус перед скобкой (30) → сумма и разность кубов, неполный квадрат (31) → дроби:
сокращается только множитель (32).

**Три решения по этому блоку.**
- **Куб показан на шести клетках, а не на восьми.** Эталон обещает «все восемь кусков куба»,
  но прямоугольник плоский: квадрат суммы уже свёрнут в три члена и умножается на два. Тройки
  выводятся счётом, объёмного прибора в классе нет и он не написан.
- **Урок 32 остаётся один** (решение методиста): в него входят сокращение и приведение к
  общему знаменателю — ровно те две ошибки, что названы в эталоне. Умножение и деление
  дробей в курс не попадают; это дыра в программе, закрывать её нужно отдельным уроком.
- **Дроби записаны линейно** (`a/6`, `(2x + 4) : 2`): дробной черты в классе нет ни как
  прибора, ни как стиля. Черта — работа слоя, не урока.

**Блок Б4 ЗАКРЫТ 2026-08-21:** уроки 18–24 собраны, все проходят `check-grade7`, аудит и
сборку. Линия блока: многочлен и его виды (18) → сложение и вычитание в столбике (19) →
многочлен на одночлен, прямоугольник в один ряд (20) → многочлен на многочлен,
прямоугольник 2×2 (21) → вынесение множителя, тот же прямоугольник читается назад (22) →
группировка, зоны и общая скобка (23) → деление, лента множителей с вычёркиванием (24).

**РЕШЕНИЕ МЕТОДИСТА 2026-08-21: новых приборов не писать, пока хватает готовых.** Дословно:
«может не будем создавать приборы а будем пользоваться тем что уже есть». Инвентарь класса —
39 приборов, и уроки 22, 23, 24 полностью легли на существующие: лента множителей (`FactorTape`,
режимы `cross` и `mixed`), зоны (`SortZones`), прямоугольник (`AreaGrid`), подстановка,
переписывание по шагам. Пересчёт по инвентарю: из 28 оставшихся уроков 26 идут на готовых
приборах. Два места, где готового нет и подмена ломает урок, названы прямо: координатная
плоскость для Б6 (функции и системы, семь уроков) и чертёж для Б7 (геометрия, девять уроков).
Решение по ним принимается, когда работа дойдёт до этих блоков.

**Обратного режима у прямоугольника нет и он не пишется.** Вынесение множителя и группировка
проверяются так: ученик умножает свой разложенный ответ и смотрит, вернулось ли исходное.
Методически это сильнее — разложение обязано умножением вернуться назад.

**У конвейера появился вид экрана `tape`** (2026-08-21) — лента множителей была написана в
Б3, но экрана в `screens.jsx` у неё не было. Добавлен экран, не прибор. Аудит о новом виде
предупреждён (`KIND_HANDS`), по правилу «меняешь форму урока — сначала правь проверку».

**УРОВЕНЬ ПРИМЕРОВ РАСТЁТ С КЛАССОМ — решение методиста 2026-08-20.** Дословно: «возраст
ученика растёт, а уровень сложности не растёт». Трудность даётся **структурой выражения**,
а не громоздкостью счёта: буквенное выражение вместо числового, знак и коэффициент вместе,
и хотя бы один пример, который нельзя решить узнаванием. Таблица уровней — в эталоне § 2.
Правило распространяется на все блоки; уже собранные уроки 1–12 подлежат ревизии уровня.

**Блок Б3 ЗАКРЫТ 2026-08-20:** уроки 13–17 собраны, все проходят четыре проверки.
Линия блока: показатель считает множители (13) → три свойства выводятся счётом (14) →
стандартный вид это сортировка ленты (15) → произведение это соединение двух лент (16) →
степень это повторение всей ленты (17). Одна механика на пять уроков. Прибор блока — `FactorTape`, лента множителей. Он взят
из определения учебника (стр. 26): степень есть произведение n множителей. Лента разводит
два уровня, которые ученик смешивает: **сумма растит коэффициент, произведение растит
показатель**. Одна механика закрывает все шесть ошибок блока.

**Урок 15 расширил ленту снова:** `mixed` кладёт в неё разнородные элементы
(`2 · a · 3 · b · a`), и стандартный вид становится **сортировкой ленты** — числа
перемножить, одинаковые буквы посчитать. Прибор при этом **не собирает ответ**: он
показывает только счёт (какие числа и сколько каждой буквы), а запись собирает ученик.

**Урок 14 расширил ленту:** `groups` делит её на группы, `cross` вычёркивает сокращённые
множители. Все три свойства становятся счётом, а не правилом для запоминания: группы
**рядом** — показатели складываются; группа **повторяется** — умножаются; множители
**вычеркнуты** — вычитаются.

**Блок Б2 ЗАКРЫТ 2026-08-20:** уроки 7–12 собраны, все проходят четыре проверки.
Линия блока: что такое корень (7) → корень вычисляют, три случая (8) → перенос как
короткая запись весов (9) → модуль как расстояние (10) → задача в уравнение, возврат к
вопросу (11) → сумма известна, вторая величина через первую (12).

**Блок Б2 начат:** уроки 7 и 8 собраны. Урок 7 закрывает тег `Z7` урока 2: там переменная
принимала любое число, здесь равенство пропускает только одно. Урок 8 даёт блоку **его
прибор** — `EquationBalance`, весы с двумя чашами, где действие применяется к обеим частям
и кнопки «только к левой» не существует (эталон § 2). Уроки 10–12 берут этот прибор готовым,
меняются только числа и набор доступных действий.

### Обвязка урока — в `core.jsx`, с 2026-08-20

`Frame` и корневой компонент дублировались в пятнадцати файлах по 140 строк. Теперь в
`core.jsx`: **`LessonFrame`** (шапка, навигация, награда) и **`createLesson`** (корень,
языки, сбор результата), плюс `collectLessonTags` и `levelFromFirstTry`. Навигационные
подписи — в `UI_TXT`.

Урок на новой обвязке кончается так:

```
export default createLesson({ id, title, no, block, tags, screens: [Screen1, …] })
```

В файле урока остаются **только данные**: содержание экранов, примеры, разборы, аудио.
Уроки 1–15 живут на старой форме и не переписывались — обе формы рабочие, скрипт приёмки
знает обе.

**Ответ на вопрос методиста «можно ли брать один скелет и менять только текст»**
(2026-08-20): обвязку — да, для этого модуль и сделан. Но **хук, разграничение, ловушку и
граничный случай заменой текста не получить** — это математика темы. В уроках 13–17 они
разные: сравнение величин, показатель, знак коэффициента, потеря буквы, коэффициент против
показателя. Если менять только текст, эти четыре экрана становятся формальными и урок
перестаёт учить.

### Апостроф в узбекской строке ловится сразу — с 2026-08-20

`check-grade7.mjs` проверяет вызовы `L(`: если узбекская строка записана в **одинарных**
кавычках и содержит ASCII-апостроф, строка обрывается и страница не открывается. Правило
проекта (CLAUDE.md § 7) существовало и раньше, но нарушалось: 2026-08-20 дважды за один
день, в уроках 14 и 15. Сборка это ловит, но она идёт 8–20 секунд, а проверка мгновенна.

Проверка сужена до `L(` и игнорирует комментарии: общий шаблон давал 4883 ложных
сообщения, а проверку с таким шумом никто не читает.

### Замер видит решённое состояние — с 2026-08-17

`grade7-noscroll.mjs` до этого дня **никогда не измерял экран после верного
ответа**. Его walker нажимал первую доступную кнопку по кругу: в заданиях со сборкой
ответа это означало «выбери первый вариант → выбери первый вариант → …», кнопка
«Проверить» не нажималась ни разу, и состояние с разбором, выводом и заполненной
таблицей оставалось неизмеренным. Именно это состояние самое тесное.

Что изменено:
- у кнопки проверки появился признак `data-check` (проп `mark` у `Btn`) — скрипт ищет её
  по признаку, а не по тексту: текст в трёх языках разный;
- walker нажимает проверку первой, а варианты **перебирает** по номеру шага, поэтому
  рано или поздно доходит до верного ответа. Правильный ответ в разметке не помечен и
  помечен не будет — иначе его увидит ученик;
- `GRADE7_FAST=1` — быстрый прогон (только ноутбук, UZ и RU) для итераций. Перед сдачей
  обязателен полный: телефонные размеры видны только в нём.

Исправленный замер сразу нашёл переполнение **в семи уроках из десяти** и один
runtime-дефект: `Transform` отдавал трёхъязычный объект в `Expr` без `t()`, и React падал
с ошибкой 31 при завершении инструмента. Ни одна проверка этого не видела.

Отсюда правило: **место в экране считается по решённому состоянию**, а не по исходному.
Разбор и вывод появляются вместе и вместе занимают до 150 px.

**Урок 10 закрыл § 10.2 эталона.** Отдельного параграфа об уравнениях с модулем в учебнике
нет, но **определение модуля есть** — стр. 6, повторение за 6 класс: модуль показывает,
на каком расстоянии число стоит от нуля. Урок целиком выведен из этого определения.
Прибор — `DistanceLine`: ученик ставит точки сам, и прибор **не считает задачу решённой,
пока найдены не все**. Ошибка «один корень вместо двух» становится невозможной так же,
как в уроке 8 стала невозможной односторонняя операция. Внешняя методика (проверено
2026-08-17) даёт то же правило: «модуль — это расстояние», и два корня выводятся, а не
объявляются.

**Урок 9** расширил прибор: переменная теперь стоит и в правой части, а действия
`+x` / `−x` вычитают её из обеих сторон. Из этого выведены **два названных способа**
учебника — аль-джабр (перенос со сменой знака) и вал-мукабала (уничтожение равных
членов). Ключевая мысль урока: перенос — **не новое правило**, а короткая запись весов,
поэтому знак и меняется. Данные уроков 7 и 8 работают без правок: `k` по умолчанию нуль.

---

## 1. Где что лежит

| Файл | Что это |
|---|---|
| `src/components/grade7/Dars01.jsx` | **эталонный урок.** Только данные: тексты, аудио, разборы |
| `src/components/grade7/Dars02.jsx` | урок 2 «Выражения с переменными», собран 2026-08-15 |
| `src/components/grade7/Dars03.jsx` | урок 3 «Свойства арифметических действий», собран 2026-08-15 |
| `src/components/grade7/Dars04.jsx` | урок 4 «Тождества», собран 2026-08-21; подстановка опровергает, преобразование доказывает |
| `src/components/grade7/Dars05.jsx` | урок 5 «Раскрытие скобок», написан с нуля 2026-08-15 |
| `src/components/grade7/Dars06.jsx` | урок 6 «Приведение подобных слагаемых», собран 2026-08-16 |
| `src/components/grade7/Dars07.jsx` | урок 7 «Уравнение и его корень», первый урок Б2, собран 2026-08-16 |
| `src/components/grade7/Dars08.jsx` | урок 8 «Линейное уравнение с одной переменной», собран 2026-08-16; здесь родился прибор блока |
| `src/components/grade7/Dars09.jsx` | урок 9 «Решение линейных уравнений», собран 2026-08-16; аль-джабр и вал-мукабала |
| `src/components/grade7/Dars10.jsx` | урок 10 «Линейное уравнение с модулем», собран 2026-08-17; прибор `DistanceLine` |
| `src/components/grade7/Dars11.jsx` | урок 11 «Решение задач с помощью уравнений», собран 2026-08-17; прибор `QuantityCard` |
| `src/components/grade7/Dars12.jsx` | урок 12 «Задачи на составление уравнений», собран 2026-08-20; **блок Б2 закрыт** |
| `src/components/grade7/Dars13.jsx` | урок 13 «Степень с натуральным показателем», собран 2026-08-20; прибор `FactorTape`, **блок Б3 начат** |
| `src/components/grade7/Dars14.jsx` | урок 14 «Свойства степеней», собран 2026-08-20; лента с группами и сокращением |
| `src/components/grade7/Dars15.jsx` | урок 15 «Одночлен и его стандартный вид», собран 2026-08-20; лента со смешанными элементами |
| `src/components/grade7/Dars16.jsx` | урок 16 «Умножение одночленов», собран 2026-08-20; **первый урок без обвязки** |
| `src/components/grade7/Dars17.jsx` | урок 17 «Степень одночлена», собран 2026-08-20; **блок Б3 закрыт** |
| `src/components/grade7/Dars18.jsx` | урок 18 «Многочлен и его виды», собран 2026-08-21; прибор `TermStrip`, **блок Б4 начат** |
| `src/components/grade7/Dars19.jsx` | урок 19 «Сложение и вычитание многочленов», собран 2026-08-21; `TermColumns` — столбик |
| `src/components/grade7/Dars21.jsx` | урок 21 «Умножение многочленов», собран 2026-08-21; прямоугольник стал 2×2 |
| `src/components/grade7/Dars22.jsx` | урок 22 «Вынесение общего множителя», собран 2026-08-21; НОД и наименьший показатель |
| `src/components/grade7/Dars23.jsx` | урок 23 «Способ группировки», собран 2026-08-21; зоны и общая скобка |
| `src/components/grade7/Dars24.jsx` | урок 24 «Деление одночленов и многочленов», собран 2026-08-21; **блок Б4 закрыт** |
| `src/components/grade7/Dars25.jsx` | урок 25 «Квадрат суммы и разности», собран 2026-08-21; **блок Б5 начат** |
| `src/components/grade7/Dars26.jsx` | урок 26 «Разность квадратов», собран 2026-08-21 |
| `src/components/grade7/Dars27.jsx` | урок 27 «Куб суммы и куб разности», собран 2026-08-21; шесть клеток дают тройки |
| `src/components/grade7/Dars28.jsx` | урок 28 «Применение формул», собран 2026-08-21; выбор формулы по признаку |
| `src/components/grade7/Dars29.jsx` | урок 29 «Разложение по формулам», собран 2026-08-21; формула наоборот |
| `src/components/grade7/Dars30.jsx` | урок 30 «Преобразование целых выражений», собран 2026-08-21; минус перед скобкой |
| `src/components/grade7/Dars31.jsx` | урок 31 «Сумма и разность кубов», собран 2026-08-21; неполный квадрат |
| `src/components/grade7/Dars32.jsx` | урок 32 «Алгебраические дроби», собран 2026-08-21; **блок Б5 закрыт** |
| `src/components/grade7/Dars33.jsx` | урок 33 «Координатная плоскость», собран 2026-08-21; **блок Б6 начат**, прибор `Plane` |
| `src/components/grade7/Dars34.jsx` | урок 34 «Понятие функции», собран 2026-08-21; две точки над одной абсциссой |
| `src/components/grade7/Dars35.jsx` | урок 35 «Линейная функция», собран 2026-08-21; первое использование `fn` |
| `src/components/grade7/Dars36.jsx` | урок 36 «Построение и чтение графиков», собран 2026-08-21 |
| `src/components/grade7/Dars37.jsx` | урок 37 «Прямая пропорциональность», собран 2026-08-21; две прямые на одном чертеже |
| `src/components/grade7/Dars38.jsx` | урок 38 «Системы уравнений», собран 2026-08-21; графический способ |
| `src/components/grade7/Dars39.jsx` | урок 39 «Подсчёт вариантов», собран 2026-08-21; **блок Б6 закрыт** |
| `src/components/grade7/Dars40.jsx` | урок 40 «Прямые и углы», собран 2026-08-21; **блок Б7 начат**, прибор `Figure` |
| `src/components/grade7/Dars41.jsx` | урок 41 «Треугольник и виды», собран 2026-08-21; вырожденный треугольник 2+3=5 на приборе |
| `src/components/grade7/Dars42.jsx` | урок 42 «Признаки равенства», собран 2026-08-21; «измерение не доказательство» включено |
| `src/components/grade7/Dars43.jsx` | урок 43 «Равнобедренный треугольник», собран 2026-08-21; без опоры на сумму углов |
| `src/components/grade7/Dars44.jsx` | урок 44 «Сумма углов», собран 2026-08-21; закономерность из измерения, доказательство в 45 |
| `src/components/grade7/Dars45.jsx` | урок 45 «Параллельные и секущая», собран 2026-08-21; перенос = доказательство суммы углов |
| `src/components/grade7/Dars46.jsx` | урок 46 «Прямоугольный треугольник», собран 2026-08-21; без Пифагора, с соотношениями |
| `src/components/grade7/Dars47.jsx` | урок 47 «Циркуль и линейка», собран 2026-08-21; клик = равные расстояния, решение единственно |
| `src/components/grade7/Dars48.jsx` | урок 48 «Итоговое повторение», собран 2026-08-21; **блок Б7 и КУРС закрыты** |
| `src/books/grade7/DARS02_SKELET.md` | раскадровка и контент урока 2, решения методиста |
| `src/components/grade7/tools.jsx` | приборы и формы ответа. Вся механика |
| `src/components/grade7/core.jsx` | вёрстка, палитра, аудио, `STYLES` |
| `src/lessons/grade7.js` | реестр: slug, тема, описание |
| `src/books/grade7/ETALON_7SINF.md` | контракт класса |
| `src/books/grade7/DARS01_HOLAT.md` | состояние урока 1, все решения и грабли |
| `src/books/grade7/PODXOD_7SINF.md` | подход к классу |
| `src/books/grade7/DARSLAR_REJASI_7SINF.md` | программа, 48 уроков |

**Урок 5 переписан с нуля 2026-08-15.** Прежний был собран по старому подходу и в новый
эталон не ложился; он удалён, новый написан заново. В реестре уроки 1, 2, 3 и 5.

Старые файлы не потеряны: `_archive/unused-code/grade7-dars05/` — компоненты и скелет.
Оттуда **не импортируют**: если материал понадобился, он осознанно возвращается в
проект (CLAUDE.md § 6.4).

**`DARS01_SKELET.md` и `DARS01_KONTENT.md` устарели** — они описывают урок, которого
больше нет. Источник истины: код и `DARS01_HOLAT.md`.

---

## 2. Главное правило файла урока

**В файле урока нет механики.** Только данные и подключение приборов.

Если в `DarsNN.jsx` появились `Options`, `Feedback`, `useSfx`, `useAnswerFx` — механика
переехала в урок, и один баг придётся чинить в каждом файле класса. Общий код живёт в
`tools.jsx` и `core.jsx`.

---

## 3. Что задаёт вид урока (решения методиста 14 августа)

Эти правила действуют на **все** уроки класса. Они уже в ядре — новый урок получает их
даром, включать ничего не надо.

### Одна колонка

Все блоки экрана стоят в одних краях: 860 px по центру. Заголовок, сцена, панели,
дорожки, разбор, лайфхак. Раньше на одном экране было до четырёх разных краёв.

Не растягивать элемент на всю ширину только потому, что место есть. Свободное место
разделяет смысловые блоки, а не живёт внутри одного элемента без функции.

### Один размер чисел

Все числа и знаки действий — одной величины: `--g7-num` в `.lesson-root`. От неё живут
запись в панели, детали ответа, варианты, дорожки, числа внутри текста задания.

Числа в прозе автоматически набираются математическим шрифтом — это делает `Fx`.

### Цвет ступени — язык урока

| Что | Цвет |
|---|---|
| `·` `:` — вторая ступень | синий, `stage2` |
| `+` `−` — первая ступень | фиолетовый, `stage1` |
| скобки | бирюзовый, `graph` |

Работает везде, где рисуется запись. Правило на экране правила собирается **из тех же
цветов** — ученик узнаёт его глазами.

Проза цвет не получает: `<Expr plain>` для вопроса, заданного словами.

### Заголовок и вопрос по центру

Ярлык «ВОПРОС» не пишется — вопросительный знак говорит то же самое. Ярлык «ЗАДАНИЕ»
остаётся: он называет действие.

### Значков «Нажмите» нет

Убраны со всех экранов. Это отменяет пункт 5 технического задания от 10 августа.

### Фон

Фон одинаковый на всех экранах, кроме экрана правила — там он остаётся оранжевым.
Хук и итог цветного фона больше не имеют.

---

## 4. Приборы

Показывают математику:

| Прибор | Что делает |
|---|---|
| `Transform` | пошаговое переписывание: ученик выбирает часть и называет действие |
| `StepOrder` | ученик расставляет порядок прямо в записи, прибор считает по его порядку |
| `BracketGap` | ученик ставит скобку; щель показывает бледную `(` |
| `CollapseTrack` | дорожка: запись сворачивается по заданному порядку |
| `CollapseFilm` | кадры объяснения, ведёт голос |
| `SolutionSteps` | полное учительское решение |
| `NumberLineTracks` | числовая прямая; в режиме `manual` шаг делает ученик |
| `HookMachines` | сцена хука урока 1: два калькулятора; `fix` делает её интерактивной |
| `RideScene` | сцена хука урока 2: дорога, две поездки, запись над ними |
| `TwoRoutes` | сцена хука уроков 3 и 5: источник, два пути, два табло, кольцо со знаком |
| `StairsReveal` | правило как лестница ступеней |
| `ReadViz` | разбор на самой записи: номера, пара, скобка |
| `HistoryTape` | лента пройденного пути |
| `TwoValues`, `LawReveal` | две карточки со знаком, закон по частям |
| `FactorTape` | лента множителей (Б3): группы, сокращение, смешанные элементы |
| `TermStrip` | полоса членов (Б4): режется только по `+` и `−`, знак уезжает с членом |
| `TermColumns` | столбик (Б4): подобные члены в один столбик, минус перед скобкой переворачивает знаки |
| `SortZones` | раскидать записи по зонам; «всё или ничего» |

Принимают ответ:

`Probe`, `ProbeChain`, `SlotFill`, `AuditRows`, `RuleGate`, `RuleBuilder`.

Форма ответа и прибор — разные вещи. Требование «экран без прибора» запрещает прибор,
а не форму ответа.

### Общие детали

`Ask` — объявление задания. `CallToAct` говорил, куда нажимать, и убран; `Ask` говорит,
**что** спрашивают. Без объявления ученик не понимает, чего от него хотят — на этом
сгорели пять экранов урока 1.

`HackNote` — лайфхак и бонус. Одна форма с разбором верного ответа, жёлтый цвет,
крупная звезда. Выходит **только после того, как ученик сделал работу**.

Звук клавиши калькулятора — `fx.tap()` на каждое нажатие внутри записи.

---

## 5. Анимация

**Движение показывает математику и появляется после действия ученика.** До ответа не
показывается ничего — иначе анимация подсказывает.

Пульсация не бесконечна: пять тактов и покой. Бесконечная анимация висит на экране
серым пятном — так было с полосой подсветки под правилом.

Только CSS и SVG. Файлов картинок в классе нет.

Число рождается везде одинаково: пара съезжается 420 мс, знак гаснет, потом приходит
результат. Один почерк на весь урок.

---

## 6. Бюджет высоты

**Прокрутки нет.** Ни на компьютере, ни на телефоне. Бюджет рабочей зоны на ноутбуке
1366×615 — **488 px**.

Экран **заполняется, а не растёт**: слот рассчитан на финальное состояние с первой
секунды. Если финальное состояние не влезает — новый шаг **заменяет** предыдущий.

Что из этого следовало в уроке 1:

- панель переписывания резервирует высоту всех строк сразу;
- на экране трапа после ответа остаются только две нужные строки;
- лайфхак и разбор не стоят рядом, если вместе не помещаются;
- резерв под будущие строки живёт **снаружи** карточки, иначе внутри видна пустота.

---

## 7. Проверки

```
node scripts/check-grade7.mjs           # апостроф, аудио, три языка. Секунды
node scripts/grade7-lesson-audit.mjs    # роли, квота, теги, разборы, реестр. Секунды
npm run build                           # обязательно перед push
npx vite --port 5261 --strictPort       # затем:
GRADE7_SLUG=<slug> node scripts/grade7-noscroll.mjs
npx vite --port 5299 --strictPort       # затем:
GRADE7_SLUG=<slug> node scripts/grade7-blitz-check.mjs
```

`grade7-lesson-audit.mjs` печатает **счётчики**, а не вердикт: экранов, квота выбора,
сколько экранов собирают ответ, сколько оцениваются. Новый урок добавляется в
`grade7-blitz-check.mjs` одной строкой в таблицу `LESSONS` — верные ответы блица и
селектор строки готовности.

Прогон идёт долго: 15 экранов × 5 размеров × 3 языка, около 3700 замеров. Урок 1
проходит его с нулём переполнений.

**Читать весь вывод, а не хвост.** Один раз я смотрел последние строки и пропустил
41 px на узбекском в начале списка.

**Снимок экрана — обязательная часть, а не роскошь:**

```
GRADE7_SLUG=<slug> GRADE7_SHOTS=1,3,7,15 GRADE7_VP=noutbuk \
  node scripts/grade7-shot.mjs      # .tmp/grade7-shot/, масштаб 2x
```

На уроке 2 он нашёл четыре дефекта, которых прогон не видел вовсе: заголовок печатался
дважды, сцена была в три раза меньше нужного, пустой резервный слот рисовался белой
карточкой, а указатель касания закрывал собой то, что показывал.

### Чего прогон НЕ ловит

- **видно ли содержимое.** Класс сегмента цифры совпал с полосой прогресса, телефонные
  стили её прячут — на телефоне не было чисел на калькуляторах, а прогон был зелёным;
- **приборы до конца.** Он жмёт первую кнопку, то есть отвечает неверно. Панель
  переписывания росла на 65 px, и он этого не видел;
- **читается ли рисунок.** Первый вариант стикера руки читался как неприличный жест.
  Такие вещи смотреть только на увеличенном снимке.

Поэтому каждый новый экран проверяется ещё и руками: пройти правильными ответами,
снять снимок, посмотреть.

---

## 8. Грабли

**Бэктик в комментарии внутри `STYLES` рушит весь файл.** Шаблонная строка закрывается,
страница отдаёт 500, проверки рапортуют «чисто», потому что мерить нечего. Ловится
`check-grade7.mjs`. Наступал семь раз.

Уточнение от 2026-08-15: **`npm run build` при этом проходит**. Остаток файла всё ещё
разбирается как JS, сборка зелёная, а страница не грузится. Симптом — прогон вёрстки падает
по таймауту `.stage-content`, и это читается как поломка скрипта, а не файла. Первым делом
в такой ситуации гнать `check-grade7.mjs`.

**`{/* … */}` между пропами JSX-элемента — синтаксическая ошибка** с тем же симптомом:
страница не грузится, селектор не дожидается. Комментарий ставится над элементом.

**Не править файлы скриптом, который открывает их на запись целиком.** Скрипт может
упасть между «обнулить» и «записать». Так был потерян `core.jsx` — 3700 строк.
Восстановлен из недостижимого объекта `git stash`.

**Столкновение имён классов между общей вёрсткой и прибором** ничем не ловится.

**Порядок правил в `STYLES` решает.** При равном весе выигрывает то, что ниже.
Центрирование вопроса не работало, пока правило стояло выше блока зон.

**Скелет блока переиспользуется на 8 экранов из 15** (вопрос методиста 2026-08-21,
«можем взять один скелет и менять только контент»). Переносятся как есть: опора 2, второе
представление 5, правило 8, практика 9–11, блиц 14, итог 15 — там роль, форма ответа и
вёрстка одинаковы. Не переносятся: хук 1, разграничение 4, «сам» 6, граничный случай 7,
ловушка 12, перенос 13 — это математика урока, а не текст. Скелет берётся **из своего
блока**: из чужого он тащит чужой прибор.

**Прибор может быть написан, но не одет.** `SortZones` лежал в `tools.jsx`, а классов
`.g7-sz-*` в `STYLES` не было вовсе. На экране это три строки текста без рамок: зоны клика
не видно, хотя это must-пункт методического профиля. Ни одна проверка этого не поймала —
разметка валидна, высота в бюджете, консоль чистая. Нашлось **только на скриншоте**.
Прежде чем брать прибор, которым класс ещё не пользовался, проверить, есть ли его классы в
`STYLES` (2026-08-21, урок 18).

**`Transform` принимает не любую «часть».** Нажимается только **знак действия** в записи,
а идентификатор части — это тройка вокруг знака, из которой убраны скобки: в `2(x + 3)`
единственный нажимаемый знак — плюс внутри скобки, и его часть называется `2x + 3`.
Написал `part: '2(x + 3)'` — на экране не осталось ничего нажимаемого, и замер сказал
«на слайде 5 нажать нечего». Плюс: объявление задания у прибора по умолчанию из урока 1
(«что считаем первым»), для другой темы его надо передать пропом `ask` (2026-08-21, урок 4).

**`TwoRoutes` кладёт токены с шагом 27 px.** Токен в 6–7 знаков наезжает на соседний, и
запись превращается в кашу (`6x2x · 3x²5` вместо `6x³ − 2x · 3x² + 5`). Токен — это один
член или один знак, не кусок записи. Тоже видно только на скриншоте (2026-08-21, урок 18).

---

## 9. Урок 5 — СДЕЛАН 2026-08-15

Ниже — задание, по которому он писался. Оставлено как запись решения.

Тема по программе — раскрытие скобок. Урок делается **с нуля**, старый не переписывается.

Что от старого урока может пригодиться как справка, но не как исходник:

| Где | Что |
|---|---|
| `_archive/unused-code/grade7-dars05/DARS05_SKELET.md` | раскадровка старого урока |
| `_archive/unused-code/grade7-dars05/Dars05.jsx` | пилот, отклонённый методистом |
| `src/books/grade7/DARS05_ANIMATSIYA_TAKLIF.md` | предложение по анимации, осталось в проекте |

Приборы для этой темы уже есть и проверены на уроке 1: `Transform` для пошагового
раскрытия, `SlotFill` для расстановки знаков, `AuditRows` для поиска первой неверной
строки, `SubstituteRows` для проверки числом. Новых писать не нужно — нужно выбрать.

После сборки урок добавляется в `src/lessons/grade7.js` и проверяется:

```
GRADE7_SLUG=<новый-slug> node scripts/grade7-noscroll.mjs
```

---

## 10. Как делать новый урок

**Скелет пишется в чат, не в файл** (решение методиста 2026-08-15). Раскадровка
показывается методисту прямо в переписке; `DARSNN_SKELET.md` заводится только по просьбе.
Причина: скелет утверждается и меняется в разговоре, а файл после первой правки
устаревает — так вышло с `DARS01_SKELET.md`.

**Ссылок на учебник в уроке нет** — ни параграфа, ни страницы, ни слова «учебник»: ни на
карточке правила, ни в бонусе, ни в разборе, ни в озвучке (`ETALON_7SINF.md` § 3.4).
Прослеживаемость живёт в скелете и контенте, не на экране.

1. **Скелет** — список экранов: роль, цель, тип взаимодействия, misconception и разбор
   на каждый неверный вариант.
2. **Контент** — RU + UZ + EN и аудио на каждый экран.
3. **Сборка** — файл урока с данными плюс запись в `src/lessons/grade7.js`.
4. **QA** — по критериям приёмки эталона.

Этапы не пропускаются. Роли экранов, квоты вариантов и обязательные теги — в
`ETALON_7SINF.md`.

**Порядок экранов урока 1**, как ориентир: хук · опора на чтение записи · порядок
ставит ученик · способ скобкой · способ по ступеням · новый случай · граничный случай ·
правило · три практики · ловушка · обратная задача · блиц · итог.

---

## 11. Что осталось не сделано в уроке 1

- `freeNav: true` — режим разработки. Перед сдачей класса `false`;
- возврат назад теряет состояние ответов (§ 5.3 эталона);
- обход не доводит приборы до конца — самый ценный невыполненный пункт;
- `DARS01_SKELET.md` и `DARS01_KONTENT.md` не переписаны под то, что есть;
- как звучит боевой TTS на трёх языках — никто не слышал, в превью заглушка.
