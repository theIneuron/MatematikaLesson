# GRADE10 HANDOFF — состояние на 2026-08-27

---

## НУМЕРАЦИЯ И НОВЫЕ УРОКИ — 27.08.2026, читать до всего остального

> **В курсе 47 уроков: 42 собрано, 5 в производстве.** Нумерация сплошная 1–47, номер урока —
> его порядок в курсе. Пять номеров зарезервированы под новые темы, взятые из учебников 2022
> года: их нет в плане xlsx и нет ни в 7–9, ни в 11 классе.
>
> | Новый урок | Тема | Опора | Где стоит |
> |---:|---|---|---|
> | **8** | Сложная, обратная и периодическая функция | «Алгебра» стр. 35–41 | после урока 7, ДО аркфункций — **СОБРАН 27.08.2026** |
> | **25** | Системы уравнений: рациональные, иррациональные, показательные, логарифмические | «Алгебра» стр. 70–73, 87–94, 119–122 | после урока 24 — **СОБРАН 27.08.2026** |
> | **30** | Параллельность прямой и плоскости | «Геометрия» §13, стр. 98–102 | после урока 29 — **СОБРАН 27.08.2026** |
> | **31** | Параллельность двух плоскостей | «Геометрия» §14, стр. 103–108 | за уроком 30 — **СОБРАН 27.08.2026** |
> | **32** | Параллельное проецирование | «Геометрия» §15, стр. 109–112 | до перпендикулярности — **СОБРАН 27.08.2026** |
>
> Порядок вставки взят из учебника, а не «в конец курса». До сборки этих пяти уроков в
> нумерации пять пропусков — это ожидаемо.
>
> **В тексте НИЖЕ номера СТАРЫЕ** — те, под которыми уроки производились. Переводить по
> таблице:
>
> | Было (в тексте ниже) | Стало |
> |---|---|
> | 1–7 | 1–7 (не изменились) |
> | 8–13 | **9–14** |
> | 14–25 | уроков нет: производная в 11 классе |
> | 26–31 | **15–20** |
> | 32 · 33 · 34 · 35 | **21 · 22 · 23 · 24** |
> | 36 · 37 | **26 · 27** |
> | 38 · 39 · 40 | **28 · 29 · 33** |
> | 41 | **36** — теорема о трёх перпендикулярах |
> | 42 · 43 | **34 · 35** — угол и двугранный |
> | 44–49 | **37–42** |
> | 50–54 | **43–47** |
> | 55 | «Задачи ДТМ» — снят с производства 27.08.2026, копия в `.tmp/removed-dars43/` |
>
> **Номер урока со строкой плана xlsx не совпадает.** Строки контроля номера не занимают,
> производная выпала, две строки дали по два урока, пять уроков строк не имеют вовсе. Перевод
> только по таблице `DARSLAR_REJASI_10SINF.md` §Сверка со xlsx; столбец «Наш урок» есть и в
> самом плане: `src/books/Math_1-11_Поурочно_RUz_v4_СВЕРКА-10.xlsx` (жёлтые строки — вторая
> половина разбитой строки, зелёные — новые темы, зачёркнутые — ушли в 11 класс).
>
> **Разбитый урок в имени файла НЕ помечен.** `Dars22.jsx` выглядит как обычный урок; то, что
> он продолжает тему урока 21, записано в комментарии над `LESSON_NO`, в описании урока в
> реестре и в примечании к таблице блока 5 плана.
>
> Что переименовано: файлы уроков, файлы контента `DARSNN_KONTENT.md`, слаги и записи в
> `src/lessons/grade10.js`, планы `grade10-hand.mjs` и `grade10-tts-check.mjs`. Что НЕ
> переименовано: файлы скелетов (`DARS32_36_SKELET.md`, `DARS38_40_SKELET.md`) — они называют
> пакет производства, а не урок.
>
> Точная таблица «строка плана → урок → файл» — в `DARSLAR_REJASI_10SINF.md`
> §Сверка со xlsx. Столбец «Наш урок» есть и в самом плане:
> `src/books/Math_1-11_Поурочно_RUz_v4_СВЕРКА-10.xlsx`.

---

## СЕССИЯ 2026-08-20: ГЕЙТ ПРИБОРА 6A ПРОЙДЕН

Подробности — `START_GRADE10.md` §0, там таблица «что было / что стало». Коротко:

1. **Прибора не было в дереве.** Параллельная сессия в 14:44 сделала `git stash` («wt: chuzhaya
   rabota, vremenno») и не вернула. `Space`, `SpinScene`, `ProofRows`, `Bag`, `BagPick` лежали в
   `stash@{0}`, уроки 37–40 импортировали пустоту, сборка падала. Из стеша возвращены слой 10
   класса и `shared/lesson-core.jsx` (его не хватало 9 классу). **Остальные 79 файлов стеша —
   работа 7, 8 и 9 классов, не трогать без методиста.** `git stash pop` даст конфликт.
2. **Прибор исправлен по девяти пунктам** — пустая кнопка ловушки, разбор на верный-но-не-туда
   выбор, двойной вопрос, подпись «потяни», стартовый ракурс, прыжок плоскости, невидимая ось
   вращения, чужие точки на кадре, бледный подсвеченный пунктир, отступ панели.
3. **Урок 39: камера обмана.** Пара рёбер не менялась. При наклоне камеры класса (26°) ребро
   нижней грани и ребро верхней **не могут** сойтись на экране — нужно больше 35°. У экрана 3 своя
   камера 40° и шаг поворота 1.2.
4. **Урок 40: перепривязаны обоснования** в столбике доказательства (общая вершина обосновывает
   пересечение рёбер, а не перпендикулярность).
5. **Кадры прибора** — `.tmp/pribor6a/` (до) и `.tmp/pribor6a-after/` (после).

**Урок 41 собран и зелёный** (контракт, три языка, сборка, вёрстка 6 размеров на 3 языка, проход
руками 4 размера, озвучка). Порядок пакета 41–43 изменён на учебниковый, расхождение записано в
план. Прибор дополнен: несколько отметок прямого угла, `scale` у отметки, `stepYaw` у `SpinScene`.

**Уроки 42 и 43 собраны и зелёные, блок 6 закрыт.** 42 — угол прямой и плоскости, 43 — двугранный
угол. Прибор дополнен: `arcAt` (дуга с подписью), свой угол поворота у каждой плоскости, `half`
(полуплоскость), `scale` у отметок.

**Прибор 6B написан** — расширение `Space`: `poly` (генератор призмы и пирамиды, основание можно
задать руками) и `faces` (закрашенные грани). Снято на стенде.

**Уроки 44 и 45 собраны** (призма, параллелепипед), опора стр. 44–45, определения дословно.
Три тега блока 7 в эталоне.

**Уроки 46, 47, 48 и 49 собраны, блок 7 закрыт.** 46 — пирамида и апофема, 47 — площадь
поверхности через развёртку, 48 — правильные призмы и пирамиды, 49 — построение сечений.
Готово 37 из 55, осталось 6 — весь блок 8, и он ждёт согласования ДТМ.

**Прибор 6B дорос до сечений:** `cuts` (точка задаётся ребром и долей на нём), `meets` (точка
пересечения двух прямых — для метода следов), `cut` (многоугольник сечения поверх рёбер), точка
без подписи. Развёртка `Net` обобщена на n-угольное основание.

**БЛОК 8 ПОДГОТОВЛЕН, НО НЕ НАЧАТ.** Уроки 50–55 ждут подписи ДТМ-специалиста. Два прерусловия,
которые от подписи не зависят, сделаны:

1. **Режим ДТМ написан** в `src/components/grade10/screens.jsx` (`DtmBody`, `DtmClock`,
   `DtmMapBody`, `makeLesson({ mode: 'dtm' })`), стенд `probe/dtm10.html`, кадры `.tmp/dtm10/`.
   Проверено на ноутбуке и телефоне: адрес урока вместо разбора, часы стоят после ответа, карта
   пробелов считается из фактических ответов.
2. **Прибор 6C писать не надо:** `SpaceFrame` 11 класса покрывает все строки блока и работает в
   оболочке 10 класса (стенд `probe/space10.html`). Подключать импортом.

Открытый вопрос методисту: в курсе окажутся две проекции — камера класса в блоках 6–7 и кабинетная
в блоке 8. Причина посчитана (вектор вида один-один-один вырождается), решение за методистом.

**После правки общего слоя перепроверено:** аудит уроков 3 и 49, вёрстка уроков 3 и 30, проход
руками урока 49 — всё зелёное.

**ТЕОРИЯ 10 КЛАССА ЗАКРЫТА 2026-08-21.** Уроки 50–55 собраны и зелёные, блок 8 закрыт. Всего
теория это 43 урока (1–13 и 26–55), и все 43 прошли аудит эталона: 43 зелёных, 0 красных.

**Счёт, который путают:** в плане xlsx 53 урока, в нашей нумерации 55 номеров, делаем 43 (уроки
14–25 — производная — в 10 классе не делаются). Расхождение 53/55 предметнику не сообщено.

**Урок 55 — набор, а не урок:** 14 задач по шести блокам года плюс карта пробелов, режим ДТМ.
Верный вариант разложен по местам, а не первым (грабля 7 класса). Сборщик и аудит получили ветку
под набор: он узнаётся по ролям (14 `drill` плюс `summary`), и в нём проверяется своё — каждая
задача обязана назвать блок и адрес урока.

**Две грабли контента, обе видны только на живой странице:**

1. **вертикальная черта** `|AB|` рвала ячейку таблицы (урок 51 не поднимался вовсе). Сборщик
   научен экранированной черте;
2. **разделитель ` · `** съедает точку умножения: `a · b` в списке разваливалось на два элемента
   (урок 53). Умножение пишется без пробелов: `a·b`. Это повтор грабли урока 47 (`P·h`).

**Правка прибора проверена на 11 классе.** После добавления пола подписям урок 35 одиннадцатого
класса (там координаты и проекции, то есть ровно те подписи, которые изменились) проходит проверку
вёрстки чисто на всех трёх языках: узбекский 586 измерений, русский 1118, английский 473 и 952 на
двух размерах — везде «toza». Прогон прервался по времени, не по дефектам.

**ГРАБЛЯ СБОРЩИКА: вертикальная черта в значении рвала ячейку.** Длина вектора пишется `|AB|`, и
это разделитель таблицы markdown. Сборщик делил строку по всем чертам, `order.items` приходил
строкой вместо списка, и урок 51 в браузере не поднимался вовсе. **Сборка проходила зелёной** —
такую ошибку показывает только живая страница, поэтому проход руками обязателен. Сборщик научен
экранированной черте.

**УРОК 50.** Маршрут `dars50-fazoda-koordinatalar`, запись в реестре есть,
планы прохода руками и озвучки дописаны. Все машинные проверки пройдены: контракт эталона, три
языка, сборка, вёрстка (15 слайдов, 6 размеров, 3 языка, 2538 измерений), проход руками на
4 размерах, озвучка. Первый урок блока 8.

**ЧЕТЫРЕ ЛОВУШКИ ПЕРЕХОДНИКА К ПРИБОРУ ДРУГОГО КЛАССА** (`src/components/grade10/space.jsx`).
Ни одна не видна по коду, все четыре найдены измерением:

1. **Язык.** Прибор берёт свои подписи из контекста 11 класса; урок 10 класса заполняет свой, и
   в узбекском уроке появлялось «расстояние». Лечится обёрткой в провайдер 11 класса.
2. **Имя ракурса.** `SpinScene` даёт сцене `yaw`, прибор ждёт `yaw0`. Без сопоставления кнопка
   поворота нажимается, вопрос открывается, а чертёж стоит на месте — экран врёт.
3. **Ширина.** У прибора `svg width="100%"`, а `Scene` 10 класса ставит фигуру во flex-центровку,
   где `width: 100%` ни на что не опирается: браузер берёт стандартные 300 px, и чертёж выходит
   вдвое меньше своей коробки. И `size`, который даёт `Scene`, тут не годится: это сторона
   КВАДРАТА (`min(ширина, высота)`), а прибор не квадратный.
4. **Пол шрифта.** Подписи прибора заданы в единицах (11, 11,5, 13) и на экране умножаются на
   масштаб. В 11 классе слот широкий, масштаб единица, 11 единиц дают 11 px. В узких слотах
   10 класса масштаб 0,83, и те же 11 единиц дают 9 px — ниже пола 10,5. Это та же грабля
   «коэффициент пробивает пол», только на слой выше.

**Все четыре закрыты. Прибор поправлен ПО РАЗРЕШЕНИЮ МЕТОДИСТА (2026-08-21).** Первые три
ловушки лечатся в переходнике `space.jsx`. Четвёртая вылечена в две руки:

- в приборе (`src/components/grade11/tools.jsx`) появился **пол у каждой подписи**: все размеры
  идут через один помощник `FS`, и меньше 11 единиц не бывает. Раньше числа на осях писались
  9,5 единицами, а нижний индекс — 9,36: они пробивали пол даже без уменьшения;
- у прибора появился **`textScale`** со значением по умолчанию единица. Внешний слой измеряет
  настоящий масштаб чертежа по матрице и передаёт обратную величину: чертёж уменьшился — подпись
  во столько же раз выросла и на экране осталась своего размера. Верхняя граница 1,8.

Значение по умолчанию единица означает, что для 11 класса ничего не изменилось, кроме подписей,
которые были МЕЛЬЧЕ пола: числа на осях и нижние индексы стали 11 единиц. Это улучшение и там.

**ЕЩЁ ОДНА ГРАБЛЯ, ДОРОГАЯ ПО ВРЕМЕНИ: dev-сервер умер молча.** Три правки подряд не меняли
результат проверки вёрстки — ровно те же 337 дефектов. Причина не в правках: сервер на 5210 упал
(memory: параллельная сборка в `.wt-*/dist` роняет его через EBUSY), а проверка честно мерила
последнюю живую страницу. **Если проверка не реагирует на правку — сначала `curl` порт.**

**ГЛАВНЫЕ ГРАБЛИ ДНЯ.**

1. **Плоскость сечения может встать к камере ребром, и сечение спроецируется в полоску.** Так на
   стенде вышли и шестиугольник, и семиугольник. Проверять надо косинус нормали плоскости с
   направлением взгляда: при камере класса направление взгляда это вектор с компонентами
   синус поворота на косинус наклона, косинус поворота на косинус наклона, синус наклона. Меньше
   примерно 0,4 — фигура на этом ракурсе нечитаема.
2. **Свидетель, срабатывающий в одном положении из восьми, это не свидетель.** Самопересечение
   ложного четырёхугольника при шаге класса 0,6 открывалось только влево. Ракурс и шаг подобраны
   перебором: 0,7 и 1,2 — рвётся в любую сторону, и куб при этом не вырождается.
3. **Проверка озвучки ругалась на слово `yotadi`.** В узбекском это «лежит», состояние, и это
   слово учебника. Урок про сечения стоит на нём целиком. Слово убрано из списка движения.
4. **В формулах не бывает слов** ни одного языка: значение из таблицы «Формулы» идёт во все три
   языка одним. Запись `ABCDEF = muntazam` показывала русскому ученику узбекское слово. Признак
   правильности пишется УСЛОВИЯМИ: `AB = BC = … = FA`. Исправлено в уроках 44 и 48.
5. **`Options` отдаёт в `onPick` ЭЛЕМЕНТ, а не его id**, и `picked` для него значит «закрыто»:
   остальные варианты сворачиваются. Поэтому неверный выбор нельзя писать в `picked` — иначе
   второй попытки не будет. Обе ошибки были сделаны в `DtmBody` и обе видны только в проходе
   руками: кнопки нажимаются, а на экране ничего не меняется.
6. **Разделитель ` · ` разрывает произведение.** `P · h` уезжало в семь плиток вместо одной, и
   экран 9 урока 47 выходил за телефон на 70 px. Умножение пишется без пробелов: `P·h`.

**Проверки, которые чинились по ходу:** `grade10-noscroll.mjs` больше не считает обрезкой
элемент, который ИСЧЕЗАЕТ (варианты после ответа сворачиваются 0,05 с задержкой, и замер
через 180 мс ловил их на середине анимации — два плавающих ложных дефекта в уроке 40).
План прохода руками урока 40 обновлён под новую привязку обоснований.

---


# Старая записка — состояние на 2026-08-16

Записка для новой сессии. **Точка входа по 10 классу остаётся `START_GRADE10.md`** — там рабочее
состояние подробно. Здесь короткая сводка и то, чего в START нет: чем закончилась эта сессия.

---

## 1. Что сделано в этой сессии

**Собрано 15 уроков: 26–40.** Все прошли машинные проверки — контракт эталона, три языка,
`npm run build`, `noscroll` (6 размеров × 3 языка), проход руками (4 экрана, конечное состояние),
озвучка с маркером и `lesson_id`.

| Уроки | Тема | Прибор |
|---|---|---|
| 26–31 | степень, показательная, логарифм и их уравнения | 2, 3, 4 упрощ., **5 (написан)** |
| 32–36 | иррациональные уравнения, рациональные неравенства, преобразование логарифмов, показательные и логарифмические неравенства, тригонометрические неравенства | 2, 4 упрощ., 5 |
| 37 | теория вероятностей | **7 (написан)** |
| 38–40 | аксиомы, скрещивающиеся, перпендикулярность | **6A (написан)** |

**Написаны три прибора.** 5 — `DomainBand` (полоса допустимых значений, лента знаков).
7 — `Bag` + `BagPick` (мешок исходов). 6A — `Space` + `SpinScene` + `ProofRows` (пространственная
сцена, SVG без WebGL).

**Решения методиста.** Компрессия строк плана 32 и 33 отклонена: развёрнуты в пять уроков, курс
стал **55 уроков вместо 53**, нумерация блоков 6–8 сдвинулась на +2 относительно xlsx (записано в
`DARSLAR_REJASI_10SINF.md` §Разбиение блока 5). Практикум повторения и практическая часть
отложены. У практикума есть документ подхода `PODXOD_AMALIYOT_10SINF.md`, **на утверждении**.

**Закоммичено частично.** Коммит `d36ff72d` закрыл уроки 26–36. Урок 37, приборы 7 и 6A,
уроки 38–40, скелеты и правки эталона — **в дереве, не закоммичены**.

---

## 2. Где производство остановлено

**На гейте плана после урока 40:** «проверка прибора 6 до массового производства». Дальше блок 6
не идёт, пока прибор не посмотрит методист. Что смотреть — `START_GRADE10.md` §0, пять пунктов.

---

## 3. Что открыто и ждёт методиста

1. **Гейт прибора 6A** — главное.
2. **Практикум повторения блока 5** — подход написан, не утверждён.
3. **Компрессия урока 39**: строка плана «параллельность», в учебнике четыре параграфа, в урок
   взято ядро. Тот же вопрос по уроку 7 висит с 14 августа.
4. **`FREE_NAV = true`** в ядре класса: замок навигации выключен на всех 28 уроках.
5. **QA глазами** — пятнадцать новых уроков машина проверила на геометрию, вёрстку, языки и
   проходимость, но не на понятность.
6. **Предметнику про сдвиг нумерации** — до сдачи блока 6.

---

## 4. Грабли, которые стоили времени именно в этой сессии

- **`Options` рисует `label` как есть.** L-объект внутри роняет экран целиком
  (`Objects are not valid as a React child`). Переводить надо до передачи: `label: t(o.label)`.
- **Обёртка фигуры обязана пробрасывать `size`.** `Scene` подставляет размер через
  `cloneElement`; обёртка вида `({ step }) => <Plane .../>` его проглатывает, и чертёж рисуется в
  размере по умолчанию. В уроке 35 это дало восемьдесят переполнений.
- **Разделитель `order.answer` — два пробела**, поэтому внутри элемента их быть не должно.
  Та же природа, что ` · ` в формулах урока 13.
- **В `match` и `order` значения обязаны быть разными.** Два одинаковых ответа делают соединение
  неоднозначным: ученик не может знать, какой к какому условию.
- **Нижние индексы рендерятся по-разному.** `Fx` (в `MatchPairs`, чипах, `Options`) превращает
  `log₂` в `log2`; `AuditRows` не рендерит через `Fx`, там остаётся символ `log₂`. План прохода
  руками это различает.
- **Проверки ждут DEV-сервер на 5210.** Если там висит `vite preview`, скрипты молча гоняют
  старую сборку, и новый урок отдаёт главную страницу сайта.
- **Длинные heredoc в bash с JSX и кириллицей рвутся.** Скрипты правок писать файлом в
  scratchpad и запускать оттуда.

---

## 5. Чем проверять

```
node scripts/grade10-lesson-audit.mjs src/components/grade10/DarsNN.jsx
node scripts/grade10-lang-check.mjs
npm run build
npx vite --port 5210 --strictPort          # DEV, не preview
node scripts/grade10-noscroll.mjs <slug>
node scripts/grade10-hand.mjs darsNN
node scripts/grade10-tts-check.mjs darsNN
node .tmp/pdftext2.mjs <pdf> <от> <до>     # чтение учебника
```
