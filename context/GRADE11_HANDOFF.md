# 11 класс — передача состояния

> Записано 2026-08-20. Точка входа для работы по 11 классу — `START_GRADE11.md` в
> корне, контракт — `src/books/grade11/ETALON_11SINF.md`. Этот файл нужен, чтобы
> новый чат вошёл в курс дела за одно чтение, и не заменяет те два.

---

## 1. Где мы сейчас

**Готово 37 уроков из 50.** Закрыты пять блоков целиком.

| Блок | Уроки | Тема |
|---|---|---|
| Б1 | 1–7 | первообразная и интеграл |
| Б2 | 9–14 | показательные и логарифмические уравнения |
| Б3 | 16–24 | комбинаторика, вероятность, статистика |
| Б4 | 26–33 | стереометрия: тела вращения и объёмы |
| Б5 | 35–41 | координаты и векторы в пространстве |

Маршрут урока: `/11-sinf/matematika/nazariy/<slug>`. Реестр — `src/lessons/grade11.js`,
37 записей, дублей нет. Каждый урок: 15 экранов, три языка, мужской голос.
Осталось произвести 13 уроков: 43–49 (Б6) и 51–56 (Б7).

ПК и ИК (8, 15, 25, 34, 42, 50, 57, 58) в производство не входят — решение методиста
2026-08-14.

---

## 2. Как устроен урок

Четыре слоя, урок трогает только верхний:

| Файл | Что внутри |
|---|---|
| `src/components/grade11/DarsNN.jsx` | ДАННЫЕ экранов, ничего больше |
| `src/components/grade11/screens.jsx` | обвязка, 15 тел ролей, `makeLesson`, `TAG_NAMES` |
| `src/components/grade11/tools.jsx` | приборы (механика), пишутся один раз на блок |
| `src/components/grade11/core.jsx` | озвучка, вёрстка, типографика, TTS, `L`, `Fx`, `isTri` |

**Приборы, которые уже есть.** Полный список — в эталоне §4.2. Крупные:

- `TransformChain` — цепочка равносильности (Б2, 9 уроков);
- `AreaBoard` — площадь под кривой, подвижная граница (Б1, уроки 4–7);
- `OutcomeTree` — дерево исходов, сумма и произведение рядом (Б3);
- `FrequencyBoard` — четыре режима: клетки, столбики, машина испытаний, облако точек (Б3);
- `SpinBoard` — тело вращения (Б4, 9 уроков): пять движений — вращение, сечение,
  развёртка (цилиндр, конус, призма), переливание, стопка дисков. **Тело крутится
  руками**: тяга вбок ведёт образующую вокруг оси, тяга вверх меняет наклон взгляда.
  Включается уроком через `solid.interactive`.
- `SpaceFrame` — пространственный каркас (Б5, 7 уроков): десять режимов — точка и её
  проекции, диагональ параллелепипеда, середина и деление в отношении, координаты
  вектора, три правила сложения, скалярное произведение с обнулением на 90°, плоскость
  с нормалью, ребро двугранного угла с обеими парами углов, честная линейка, движения и
  гомотетия. Проекция кабинетная, как в учебнике; каркас поворачивается руками.

---

## 3. Чем проверять

Четыре скрипта плюс стенд. Первые три — секунды, четвёртый — минуты.

```
node scripts/grade11-lesson-audit.mjs                 # контракт эталона: роли, теги
node scripts/grade11-audio-lang-check.mjs             # языки, выдержка кадров, символы
node scripts/grade11-styles-check.mjs                 # стили
GRADE11_PORT=5263 GRADE11_LANGS=ru GRADE11_ONLY=telefon-393 \
  node scripts/grade11-noscroll.mjs dars27            # вёрстка
```

**`noscroll` НЕ поднимает сервер сам** — нужен запущенный
`npx vite --port 5263 --strictPort`. Без него `ERR_CONNECTION_REFUSED` выглядит как
поломка урока, а урок цел. И наоборот: брошенные dev-серверы копятся и съедают
память, после чего новый не стартует вовсе.

**Стенд `probe/spin.html`** (сервер на 5299) — смотреть чертёж ДО сборки урока.
За блок Б4 он поймал шар-сигару, непрозрачные заливки, развёртку не в масштабе,
ложную дугу внутри тела и призму, нарисованную как цилиндр. Ни один из этих
дефектов проверка вёрстки увидеть не может: она меряет габариты, а не смысл.

---

## 4. Открытый долг — читать перед сдачей класса

0. **Блок Б5 проверен на телефоне 393×660 во всех трёх языках** — все семь уроков
   чисты (по 1500 замеров на урок), урок 35 плюс ноутбук 1366×615 в двух языках.
   Планшет, монитор и два оставшихся телефонных размера не мерялись. Это тот же долг,
   что у Б4, и он теперь распространяется на 15 уроков.


1. **Языки.** Уроки 26 и 29–33 проверены только на русском. Узбекский длиннее
   русского на 10–20%, обрезки там почти наверняка есть. Уроки 16–24, 27, 28 —
   на трёх языках.
2. **Только телефон.** Ни один урок Б4 не мерялся на планшете и десктопе. Полный
   контракт — шесть размеров × три языка.
3. **`FREE_NAV = true`** в `core.jsx` — режим разработки. Перед сдачей `false`.
4. **Геометрия учебника не загружена.** По урокам 28–33 источника нет (решение
   методиста 2026-08-15 — идти без учебника). Узбекские термины стереометрии
   стоят как **draft** и требуют валидации узбекским методистом математики.
   Есть только «Призма и цилиндр» (ч. 1, стр. 146–184) и объёмы через интеграл
   (ч. 2, стр. 4–8).

---

## 5. Грабли, которые дороже всего стоили

- **`L()` в данных прибора кладёт экран.** Выбирают не только символы, но и слова
  («медиана», «общая причина»). Закрыто внутри приборов через `isTri()`:
  `SignFill`, `BuildExpr`, `AnswerValue`. Ключ подсказки допускает форму массива.
  Признак в консоли: сначала «two children with the same key», потом «Objects are
  not valid as a React child».
- **Хуки выше ранних `return`.** В `SpinBoard` пять режимов и два ранних выхода;
  состояние поворота обязано стоять первыми строками, иначе React падает при
  смене режима.
- **Кириллица внутри узбекских слов** (`emас`, `necha` с русской «а») — за сессию
  пять раз. Глазами неразличимо, ловит только скрипт озвучки.
- **Числа рисунка задаёт урок, а не `Math.random`** — иначе каждая загрузка даёт
  другую картинку и проверка вёрстки не ловит ничего.
- **Не коммитить на границе.** Урок 33 один прогон показал чисто, следующий — 9 px
  превышения. Нужен запас, а не одно зелёное измерение.
- **Ортографическая камера съедает диагонали.** Первая версия `SpaceFrame` рисовала
  вектор (2; 2; 1) длиной **9 пикселей**: камера над первым октантом смотрит по
  направлению со всеми положительными координатами, и такие векторы почти совпадают с
  осью взгляда. Прибор переведён на кабинетную проекцию учебника (`y` вправо, `z` вверх,
  `x` под 45° влево-вниз вдвое короче) — в ней вырождаются только направления около
  (2,83; 1; 1). Порог 24 px проверяет стенд. **Смещение между двумя равными векторами
  тоже вырождается** — их стрелки легли одна на другую, пока смещение не сменили.
- **Единица нормали и свободный член.** В режиме `dihedral` нормали приводятся к единице,
  а свободный член на ту же длину делить забыл — вершина угла оказалась на полшага в
  стороне от ребра. Ни одна машинная проверка этого не видит, нашлось на снимке стенда.

---

## 6. Что дальше

**Блок Б5, уроки 35–41: координаты и векторы в пространстве.** Раскадровка блока
сделана (в чат, 2026-08-20), прибор написан, **все семь уроков собраны**. Следующий
блок — Б6 (43–49, режим ДТМ).

**Уроки блока и их источник:** 35 (учебник 113–114, 116), 36 (122–125), 37 (126–128),
38 **без источника**, 39 **без источника**, 40 частично (115, задачи 8–11), 41 (133–139).

**Что даёт учебник.** Ч. 1, глава I, стр. 113–142, четыре раздела: 1 система координат
(113–121: декартова СК и октанты, расстояние между точками, уравнение сферы, середина
отрезка и деление в отношении λ), 2 векторы и действия (122–132: координаты вектора,
длина, сумма по четырём правилам, умножение на число, коллинеарность и компланарность,
орты, скалярное произведение, условие перпендикулярности), 3 преобразования и подобие
(133–141: перенос, три симметрии, поворот, гомотетия), 4 проверочный тест (142).

**Уроков «Уравнение плоскости» (38) и «Угол плоскостей» (39) в учебнике нет** — ни в
части 1 (оглавление, стр. 191), ни в части 2 (поиск по текстовому слою `tekislik
tenglama`, `normal vektor`, `ikki tekislik orasidagi`, `nuqtadan tekislikkacha`: ноль
совпадений на 192 страницах). Решение методиста 2026-08-20: **писать без источника**.
Нормаль и уравнение плоскости выводятся из скалярного произведения урока 37; термины
`tekislik tenglamasi` и `normal vektor` остаются **draft**.

**Темы блока** (по плану, реестр не тронут): 35 координаты · 36 действия над векторами ·
37 скалярное произведение · 38 уравнение плоскости · 39 угол плоскостей · 40 расстояния ·
41 преобразования и подобие (один урок).

**Прибор `SpaceFrame`** — в `tools.jsx`, десять режимов, подключён в `screens.jsx` через
ключ `data.space`; шаги раскрытия задаются массивами `sumSteps`, `mapT`, `lambdas`,
`feet`. Стенд — `probe/space.html` (сервер на 5299), проверки —
`scripts/_probe-space-measure.mjs` (порог длины стрелки 24 px) и
`scripts/_probe-space-zoom.mjs` (CARD=N, увеличение одной карточки).

Терминология блока взята из **узбекского** издания (стр. 113, 115, 116, 122–128,
133–138), поэтому пометки draft не требует: `oktanta (nimchorakka)`, `abssissa`,
`ordinata`, `applikata`, `kesmaning o'rtasi`, `vektorning koordinatalari`, `nol vektor`,
`erkin vektor`, `vektorning uzunligi (moduli)`, `uchburchak / parallelogramm /
ko'pburchak / parallelepiped qoidasi`, `vektorning songa ko'paytmasi`, `kollinear`,
`komplanar`, `ortlar`, `ortlar bo'yicha yoyish`, `skalyar ko'paytma`, `perpendikularlik
sharti`, `harakat`, `parallel ko'chirish`, `markaziy simmetriya`, `tekislikka nisbatan
simmetriya`, `burish`, `simmetriya elementlari`, `o'xshashlik almashtirishi`,
`gomotetiya` (термин узбекской школы, подтверждено методистом).

После Б5 остаются Б6 (43–49, режим ДТМ) и Б7 (52–56, повторение).

**Коммиты.** В рабочем дереве постоянно лежат чужие правки (классы 3, 4, 6, 7, 10,
`CLAUDE.md`, `package.json`) и работают параллельные сессии в других worktree.
Коммитить только пути 11 класса:
`src/components/grade11/ src/books/grade11/ src/lessons/grade11.js START_GRADE11.md
scripts/grade11-*.mjs probe/spin.*`

---

## БЛОК Б6 (43–49) — ЗАКРЫТ 2026-08-21

**Что это за блок.** Повторение перед ДТМ — и одновременно место, где закрылась дырка
курса: производной не было ни в одном из 37 уроков, хотя учебник отводит ей 36 часов
из 46 в алгебре (ч. 1, гл. I, стр. 3–78). Решение методиста 2026-08-21: производная
получает три урока внутри Б6, остальные темы сдвигаются.

| № | Урок | Анатомия | Источник |
|---:|---|---|---|
| 43 | Производная: скорость изменения | эталон §1 | учебник ч. 1, стр. 3–23 |
| 44 | Таблица производных и правила | эталон §1 | учебник ч. 1, стр. 24–33 (таблица на 27) |
| 45 | Касательная и исследование функции | эталон §1 | учебник ч. 1, стр. 34–49 |
| 46 | Преобразования: пробный ДТМ | эталон 1.2 | уроки курса 9–14 |
| 47 | Уравнения и неравенства: пробный ДТМ | эталон 1.2 | уроки курса 9–13 |
| 48 | Графики и площадь: пробный ДТМ | эталон 1.2 | уроки курса 1–7, 43–45 |
| 49 | Текстовые задачи и итог года: пробный ДТМ | эталон 1.2 | весь год, по задаче из блока |

**Прибор блока — `SecantBoard`** (`tools.jsx`, после `SpaceFrame`). Четыре режима:
`secant` (точка B скользит к A, разностное отношение числом), `speed` (средняя против
мгновенной), `tangent` (касательная и её уравнение, наклон читается треугольником),
`sign` (лента знака производной под графиком). Наклон считает `ddx` — из самой
функции, в данных урока ответа нет. Ключ данных — `data.secant`, ветка в `GraphBody`
перед `data.space`; `signs` и `marks` открываются по `showAt`.

`keepSecant: 'first'` — на последнем кадре остаётся САМАЯ ШИРОКАЯ секущая, а не
последняя: при малом h секущая и касательная сливаются, и кадр «средняя 4,25 против
мгновенной 4» перестаёт что-либо показывать.

Стенд — `probe/secant.html` (порт 5298), проверка `scripts/_probe-secant-measure.mjs`:
каждая подпись внутри viewBox и печатаемое число равно аналитическому.

**Карта пробелов.** `saveGaps` / `readGaps` / `topGaps` в `core.jsx` (localStorage,
ключ `g11_gaps_v1`), запись — в `makeLesson.finish`, то есть во всех уроках класса
сразу; показ — компонент `GapMap` в `screens.jsx` по флагу `gapMap` на итоге.

**Режим ДТМ.** Объявляется строкой `const MODE = 'dtm'` в файле урока и передаётся в
`makeLesson({ mode })`. Аудит читает объявление и сверяет урок с таблицей эталона 1.2
(иначе — с таблицей §1). Подпись раздела на экранах 2–14 задаётся полем
`section: 'practice'`, потому что по роли `points`, `graph` и `twoway` относятся к
объяснению, а в режиме ДТМ объяснения нет.

**Закрытый дефект в сданном.** Все 23 тега блока Б5 не были названы в `TAG_NAMES`, и
итог печатал «Слабое место:» с пустотой. Теги добавлены; в
`scripts/grade11-lesson-audit.mjs` появилась проверка «тег назван в `TAG_NAMES`» —
она поймала бы Б5. Новый тег теперь = строка в эталоне §5 **плюс** строка в
`TAG_NAMES`.

**Грабли блока.**

- строка `start` у роли `chain` и `left` у роли `sign` рисуются формулой и **не
  переносятся**: английская фраза в `start` вылезла на 89 px, русская в `left` — на
  52. Держать до ~30 знаков, вопрос произносит озвучка;
- текст карточки `twoway.txt` тоже не переносится, а колонка на телефоне ~170 px:
  предел ~22 знака в каждом языке. Русская фраза в 39 знаков вылезла на 108 px;
- экран роли `points` — самый тесный по высоте: инструкция, два утверждения, четыре
  строки и вопрос. Русский вариант выходил за бюджет на 76 px. Сокращать `rule`,
  имена утверждений и `calc`;
- роль `newcase` требует `probe1` и `probe2`, а не `probe`: с `probe` экран падает с
  «Cannot read properties of undefined (reading 'cols')»;
- `probe2` у `newcase` — это ПРОГНОЗ, он разрешается на следующем экране и попадает
  в `predicts` на итоге.

**Долг блока.** Проверено на телефоне 393×660 в трёх языках. Планшет, монитор и два
остальных телефонных размера не мерялись — тот же долг, что у Б4 и Б5, теперь на 22
уроках.

---

## БЛОК Б7 (51–56) — ЗАКРЫТ. КЛАСС СОБРАН ЦЕЛИКОМ: 50 УРОКОВ

**Что это за блок.** Повторение геометрии и итоговый ДТМ — последний блок класса.
Все шесть уроков в режиме ДТМ (эталон 1.2): задача сразу, объяснения нет.

| № | Урок | Источник |
|---:|---|---|
| 51 | Планиметрия | **без источника в учебнике 11 класса** (планиметрия — 7–9 классы); собран по канону ДТМ |
| 52 | Стереометрия | ч. 2, V глава, стр. 177–187 (банк вступительных экзаменов) + Б4 |
| 53 | Векторы и координаты | ч. 2, V глава, стр. 177–178 (банк) + Б5 |
| 54 | Тренировочный вариант | все блоки, по задаче из каждого |
| 55 | Комплексная практика | алгебра внутри геометрии: корень, степень, уравнение, производная |
| 56 | Повторение и итог курса | самые частые заблуждения года по списку тегов эталона §5 |

**Найденный источник.** Часть 2 учебника, V глава «Stereometriyani takrorlashga doir
test topshiriqlari» (стр. 177–187) — задания вступительных экзаменов в вузы, по четыре
варианта, ровно формат ДТМ. Уроки 52 и 53 берут оттуда и задачи, и неверные варианты.
**Печатного ключа к этому тесту нет:** таблица на стр. 188 относится к другому тесту
(в задании 1 она даёт D, а сумма векторов даёт B). Все ответы считаны и проверены
скриптом.

**Прибор блока — `PlaneBoard`** (`tools.jsx`, после `SecantBoard`). Плоский чертёж,
управляется данными, а не режимами: `pts`, `segs`, `angles`, `circles`, `fills`,
`marks`; ключ данных — `data.plane`, ветка в `GraphBody` перед `data.secant`.
Масштаб единый по двум осям (иначе окружность станет эллипсом, а прямой угол —
тупым), окно кадра считается из содержимого, поля сверху и снизу равны (подпись под
нижней точкой отступает на 16 px и не влезала в 14). Стенд — `probe/plane.html`
(порт 5297), проверка `scripts/_probe-plane-measure.mjs`: подписи внутри viewBox и
отношение нарисованных сторон равно отношению в данных (3 : 4 : 5 на экране остаётся
3 : 4 : 5 — это и проверяет единый масштаб).

**Новые теги (7, планиметрия):** `pyth_check`, `height_vs_median`, `mid_line`,
`inscribed_angle`, `similar_area`, `tangent_radius`, `circle_len_vs_area` — в эталоне
§5 и в `TAG_NAMES`.

**Долг класса.**

1. Вёрстка: эталон §10.1 требует шесть размеров. Б6 промерен на трёх (393×660,
   1366×615, 360×690), Б7 — на одном (393×660). Б4 и Б5 — на одном.
2. `FREE_NAV = true` (режим разработки) — перед сдачей класса `false`.
3. `tekislik tenglamasi` и `normal vektor` (уроки 38–39) остаются **draft**: их нет в
   учебнике, нужен узбекский методист.
4. Урок 51 собран **без источника в учебнике 11 класса** — по канону ДТМ. Тот же
   статус, что у уроков 38 и 39.
