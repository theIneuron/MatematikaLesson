# START_GRADE3 — точка входа по 3 классу

> Этот файл читается ПЕРВЫМ по любой задаче 3 класса (см. `CLAUDE.md` §0.4).
> Обновлён: **2026-08-05**. Последний коммит по 3 классу: `45630e2`.
> Всё, что ниже, — рабочее состояние: решения методиста, состояние уроков, техника сборки
> и процедура проверки. Новая сессия может продолжать, не поднимая историю чата.

---

## 1. ГДЕ МЫ СЕЙЧАС

**Готовы и опубликованы (теория, 3 класс):** уроки **1–14**.
Реестр: `src/lessons/grade3.js`, массив `grade3Nazariy`. Маршрут:
`/3-sinf/matematika/nazariy/<slug>`.

| № | slug | тема | коммит |
|---|---|---|---|
| 1–10 | … | нумерация, сравнение, письменные +/−, римские, таблица × | состояние от 20.07 (`afbea5c` — откат) |
| 11 | `dars11-kopaytirish-bolish-10-100` | ×÷ 10 и 100 | `aa39fb3` |
| 12 | `dars12-yigindini-kopaytirish` | умножение суммы, мост в столбик | `b7c17c8` |
| 13 | `dars13-yigindini-bolish` | деление суммы, мост в уголок | `0bedcbe` |
| 14 | `dars14-amallar-tartibi` | порядок действий, столбики × + − | `c10048e` |

Правки после сдачи: `4778f97` (факткарда 13 + финальный слайд 12–13),
`fb35f0d` (первый слайд 11–14 к эталону), `45630e2` (светлая дневная сцена, корзины
сверху, без анимации плит в 12–14).

**Практика (амалиёт):** уроки 1–19 (`grade3Amaliy`), состояние от 23.07. Практика 20–51
удалена при откате (`afbea5c`), заново не делалась.

**СЛЕДУЮЩИЙ ШАГ:** урок **15 «Связь компонентов»** (`a × b = c → c : a = b`).
Этап 1 — скелет на утверждение методиста (см. §5, порядок работы). Скелет ещё не показан.

После 15-го по программе: 16 задачи, 17 делители и кратные, 18 повторение Б2 + ПК2.
Программа: `DARSLAR_REJASI_1-11.md`, строки 160–178. Сюжет: `src/books/grade3/SYUJET_3SINF.md`.

---

## 2. РЕШЕНИЯ МЕТОДИСТА — ДЕЙСТВУЮЩИЕ ПРАВИЛА

Эти правила касаются **всех** уроков, а не только тех, где были высказаны.

**Вопросы и ответы**
1. **Ровно 4 варианта, сеткой 2×2.** Пять вариантов запрещены. Исключения: закрытое поле
   («верно / неверно», «больше / меньше / равно») и прогноз на крючке.
2. **Неверный ответ не переводит к следующему вопросу.** Индекс не двигается, балл только
   за первую попытку (`wrongSet` + `hintMsg` + `numTriedRef`).
3. **Варианты перемешиваются** при каждом монтировании (`shuffleArr`), верный не стоит
   предсказуемо первым.
4. **Верный вариант загорается зелёным** (`okPick`, класс `option-correct`, 1200 мс).
5. На каждый неверный вариант — **свой разбор**, который указывает на признак, а не даёт
   ответ.

**Факткарда**
6. **Тема — математика и наука.** Чистая биология или садовая «интересность» отклоняется
   (факт «корни пьют воду» в уроке 13 заменён на шестиугольники в сотах). Проверять три
   условия: есть математика (форма, число, порядок, доказательство) или точная наука;
   факт связан с темой урока; формулировка на языке 3 класса.
7. Факткарда стоит **под зелёной рамкой Бита**, отдельным блоком, на том же экране, что и
   последний вопрос финала. Своего экрана у неё нет.
8. Анимация факткарды — орбитальная (`lumo-orbit-front/back`, `rd-glow`, `star-tw`).

**Первый слайд (крючок) и финал**
9. **Сцена биома обязательна на крючке** и в размере эталона: урок 1, экран 0 = 629×330
   при 1440×900. Класс `.dNN-hook-scene` с бюджетом 570px; для финала `.dNN-final-scene`
   с бюджетом 560px.
10. **Панель реквизита на крючке закрывается после ответа** (`{picked === null && …}`) —
    иначе реакция Бита выдавливает содержимое в скролл.
11. **Фон светлый, дневной.** Тёмные и сумеречные сцены отклоняются: небо голубое, солнце,
    облака, зелёные холмы, тёплая светлая земля. Если сюжет требует вечера — сначала
    спросить методиста.
12. **Анимации реквизита на крючке нет** (пульсация огней выключена): внимание на вопросе.
13. Корзины и ёмкости рисуются **видом сверху, как в 1 классе** (`grade1/Dars01.jsx`,
    `BasketArt`): ручка-дуга, плетение, овальный обод, содержимое видно внутрь.

**Столбики**
14. Знаки `+ − ×` стоят **строго между числами**, как в книге. Эталон вёрстки —
    5 класс, уроки 3–4 (`grade5/Dars04.jsx` `MulColumnStepwise`, `grade5/Dars05.jsx`
    `DivBoard`). Каждая строка = слот знака (2 монопространственных символа) + тело;
    разряды строго друг под другом, перенос над своим разрядом.

**Навигация и контент**
15. **`FREE_NAV = true` пока сознательно.** Блокировка экранов включается, когда будут
    готовы все уроки 3 класса. Причина: при выключенном звуке `reached` не двигается и
    ребёнок застревает (`useAudio.muted` — состояние экрана, сбрасывается при переходе).
16. **Контент всех уроков — в одном файле** `src/books/grade3/KONTENT_3SINF.md`, новый
    урок добавляется разделом «Dars NN». Отдельные `DARSNN_CONTENT.md` не создавать.
17. Перед созданием каждого урока — **скелет на утверждение**, прозой: что на экранах,
    что в озвучке, какие анимации и механики. Методист читает текстом.
18. Русский — «ты», прошедшее время без привязки к полу; узбекский — «siz», латиница,
    апострофы ASCII.
19. Улучшать, а не упрощать: «соддалаштириш» не предлагать.

---

## 3. АРХИТЕКТУРА УРОКА (как устроен файл)

Один урок = **один самодостаточный** `.jsx` в `src/components/grade3/`, ~6000 строк.
Порядок блоков (на примере `Dars14.jsx`):

| блок | что внутри |
|---|---|
| шапка | комментарий: тема, сюжет, ядро, механики, misconception, источник контента, FREE_NAV |
| ИНФРА | тема `T`, TTS-конфиг, `useSfx`, контексты, `useAudio`, `useCanAnswer`, `useAdvanceGate`, `useRevealScroll`, `Stage`, `NavBack/NavNext` |
| META | `TOTAL_SCREENS`, `LESSON_META`, `SCREEN_META` |
| хелперы | `shuffleMC`, `shuffleArr` |
| CONTENT | `CONTENT` (RU+UZ), `BRIDGES`, `S14_PAYOFF`, `READY_LABEL` |
| КИТ | анимационный кит 1 класса, `GradientDefs`, `BitSVG`, `Reaction`, `Chiroq`, `LUMO_CAST`, `ReadinessMeter`, `GardenTerraceBg`, `LessonScene`, `ArrayViz`, `NumPad`, `MCRoundD2`, `useTapSteps`, `CountdownClock` |
| визуалы урока | только то, что нужно этому уроку (`OrderBoard`, `BasketFig`, `FoldRow`, `ColumnCalc`, факткарда) |
| экраны | `Screen0…Screen14` |
| root | `export default function …Lesson`, `finishLesson`, массив `screens` |
| STYLES | шаблонная строка CSS |

**Сборка нового урока — сплайс, а не копипаста руками.** Скрипт-ассемблер берёт из
предыдущего урока байт-в-байт инфру и кит, вставляет новые META/CONTENT/визуалы/экраны/root
и дописывает CSS. Каждая точка разреза защищена `expect(строка, подстрока)`, иначе скрипт
падает. Пример: см. описание в §6 (в scratchpad прошлой сессии `assemble-d14.mjs`).

**Дефекты движка, о которых надо помнить**
- `audio.muted` — состояние ЭКРАНА, сбрасывается при переходе. Последовательное раскрытие
  нельзя вешать на звук: только на **нажатие** (`useTapSteps` + `on_event:stepN`).
- `isPlaying` залипает в `true`, если TTS не выдал `onend`.
- `.lm-scene` — общее правило на весь урок: поднимая бюджет высоты ради тесного экрана,
  сжимаешь ВСЕ сцены урока. Правильно — класс на конкретный экран (§2, п. 9).
- В `src/lessons/grade3.js` **два массива**: `grade3Nazariy` и `grade3Amaliy`. Урок,
  вставленный перед `dars01-amaliyot`, попадает в практику и маршрут `/nazariy/…` не
  работает (сайт кидает на главную).
- Обратные кавычки внутри CSS-комментария закрывают шаблонную строку `STYLES` и ломают
  сборку.
- Патч-скрипты не должны конвертировать LF в CRLF (иначе дифф на 10 000 строк).

---

## 4. ПОВТОРЯЕМЫЕ МЕХАНИКИ (что уже написано и можно брать)

| механика | где | что делает |
|---|---|---|
| `MCRoundD2` | во всех | раунд из N вопросов: перемешивание, зелёный на верном, разбор на каждый неверный, счёт по первой попытке |
| `NumPad` | во всех | набор ответа цифрами + своя кнопка «Проверить» |
| `useTapSteps` | 11–14 | пошаговое раскрытие по нажатию, каждый шаг озвучивается `on_event:stepN` |
| `CountdownClock` | 11–14 | 5 секунд на подумать, потом варианты |
| `RazryadShiftBoard` | 11 | сдвиг цифр по разрядам при ×÷ 10/100 |
| `PathField` / `SplitArray` | 12, 13 | тропинка = 2 плиты + 3 камешка; разрез тропинки на части |
| `ColumnMulDemo` | 12 | умножение в столбик с переносом |
| `ShareOutBoard` | 13 | раздача камней из ящика по трём тропинкам |
| `DivBoard` | 13 | деление уголком (порт из `grade5/Dars05.jsx`) |
| `FoldRow` | 14 | свёртка выражения: пара загорается → плашка результата → запись короче |
| `ColumnCalc` | 14 | один столбик для `+ − ×`, перенос над разрядом |
| `BasketFig` | 14 | корзина видом сверху (наследует `grade1` `BasketArt`) |
| `OrderBoard` | 14 | доска заказа (деревянная вывеска с выражением) |

**Порт из 5 класса:** `DivBoard` и `MulColumnStepwise` переносятся почти как есть, но в
5 классе `lineHeight: 1`, а у нас 1.25 — минус вычитания уезжает на строку выше. Лечится
`top: 50%` вместо `top: 0`.

---

## 5. ПОРЯДОК РАБОТЫ ПО УРОКУ

1. **Скелет** — прозой методисту на утверждение: сюжет, 15 экранов (роль каждого), ядро
   (числовой конфликт), механики, анимации, что в озвучке, бонус, факткарда. Ждать «ок».
2. **Контент** — раздел «Dars NN» в `src/books/grade3/KONTENT_3SINF.md`: RU+UZ на каждый
   экран, озвучка сегментами, разбор каждого неверного варианта, тексты правила
   (экранный и речевой варианты отдельно), финальные 5 вопросов, факткарда, итог.
3. **Сборка** — ассемблер, запись в `src/lessons/grade3.js` (в `grade3Nazariy`!),
   `npm run build`, `npx eslint` (критично: 0 `no-undef` и 0 `Parsing error`).
4. **QA** — прогон Playwright по всем 15 экранам в RU и UZ **со звуком, выключенным на
   каждом экране**, плюс замер скролла на 1440×900, 1366×768, 390×844. Отчёт методисту.

**Требования к озвучке:** аудио шире экранного текста; без кавычек, длинных тире и
двоеточий перед перечислением; без символов `× ÷ = < > % $` — только словами
(«умножить», «плюс», «разделить»); числа словами; один сегмент = одна мысль.

---

## 6. КАК ПРОВЕРЯТЬ (процедура QA)

```bash
npm run build                        # обязательно перед коммитом
npx eslint src/components/grade3/DarsNN.jsx -f json   # смотреть no-undef / Parsing
npx vite --port 5179 --strictPort    # превью
node scripts/grade3-geometry.mjs     # геометрия сцены + скролл до/после ответа
```

`scripts/grade3-geometry.mjs` (в репозитории) печатает для списка уроков: размер
`.lm-scene`, переполнение `.stage-content` до ответа и после ответа, высоты рамок.
Эталон для сверки — урок 1: сцена 629×330, переполнения нет ни до, ни после.

**Интерактивный прогон** пишется под конкретный урок (шаблон — `walk-d14.mjs` из
scratchpad, при новой сессии восстанавливается по этому описанию):
- открыть маршрут, **выключать звук на каждом экране** (`button[title="Sound off"]`),
  переключить язык;
- проходить экраны настоящими кликами (`force: true` — пульсация кнопок не «успокаивается»);
- на тестовом экране специально ответить неверно и убедиться, что счётчик не сдвинулся,
  а верный ответ загорелся зелёным;
- на каждом экране считать `scrollHeight − clientHeight` у `.stage-content`;
- собирать `pageerror` и `console.error`;
- скриншотить каждый экран.

Готово = 15/15 экранов, консоль чистая, скролла нет на трёх размерах, оба языка.

---

## 7. ОТКРЫТЫЕ ХВОСТЫ

1. **Урок 15** — скелет не показан методисту. Это следующая задача.
2. **QA уроков 11–14 как этап 4** формально не оформлялся отчётом (прогоны делались,
   отчёт был в чате).
3. **Урок 10, первый экран:** скролл +25px при 1440×900. Состояние от 20.07, методист
   ограничил задачу уроками 12–14 — не трогали. Правится тем же приёмом (§2, п. 9–10).
4. **`DISTRAKTOR_DRAFT_3SINF.md`** — 28 предложенных четвёртых дистракторов для старых
   уроков ждут решения методиста; там же список мест, где четвёртый вариант невозможен.
5. **`FREE_NAV=false`** нельзя включать до починки сброса `muted` в `useAudio` (см. §3).
6. **Мёртвый код:** `QuestionScreen` (~110 строк) не используется ни в одном уроке 3 класса;
   в уроках 11–14 остались неиспользуемые куски инфры (eslint даёт ~69 предупреждений
   `no-unused-vars`, критичных ошибок нет).
7. **Практика 20–51** не восстановлена после откката — решение методиста не запрашивалось.

---

## 8. ГДЕ ЧТО ЛЕЖИТ

| файл | что |
|---|---|
| `src/books/grade3/KONTENT_3SINF.md` | контент всех уроков (разделы «Dars 11»…«Dars 14») |
| `src/books/grade3/SYUJET_3SINF.md` | сюжет: планета Лумо, 6 регионов = 6 блоков, вход/выход каждого урока |
| `src/books/grade3/DISTRAKTOR_DRAFT_3SINF.md` | черновик четвёртых дистракторов |
| `src/lessons/grade3.js` | реестр: `grade3Nazariy` + `grade3Amaliy` |
| `src/components/grade3/DarsNN.jsx` | уроки |
| `src/components/grade3/practice/darsNN/` | практика |
| `DARSLAR_REJASI_1-11.md` | программа (3 класс — строки 160–178) |
| `src/books/MOBIL_DESKTOP_MOSLASH.md` | контракт мобильной и десктопной вёрстки |
| `MIGRATION_v5_2_math.md` | контракт TTS v5.2 |
| `.tmp/sites-pairs-4cf9a2e/src/books/grade3/ETALON_3SINF.md` | копия эталона-контракта 3 класса (в `src/books/grade3/` его нет после отката) |

**Эталон уровня по коду:** `src/components/grade3/Dars01.jsx` (крючок, финал, гейты) и
`Dars02.jsx`. Эталон столбиков: `src/components/grade5/Dars04.jsx`, `Dars05.jsx`.
Эталон корзины видом сверху: `src/components/grade1/Dars01.jsx` (`BasketArt`).

---

## 9. ЧТО СКАЗАТЬ В НОВОЙ СЕССИИ

Достаточно: **«продолжаем 3 класс по START_GRADE3.md, урок 15»**.
Дальше по §5: сначала скелет на утверждение, потом контент, сборка, QA.
