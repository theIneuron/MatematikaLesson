// Dars 20 · Amaliyot 01 — mustaqil jsx-question komponenti.
import { createPracticeQuestion } from '../QuestionFactory.jsx';
import { DARS20_BANK } from '../newBanks.js';

export default createPracticeQuestion(DARS20_BANK.items[0]);
