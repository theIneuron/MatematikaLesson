/**
 * TEMPORARY QA SWITCH FOR GRADE 3.
 *
 * `true` keeps lesson navigation open while the grade is being reviewed.
 * Set this to `false` before the production acceptance run.
 */
export const GRADE3_REVIEW_MODE = true;

export const GRADE3_REVIEW_BADGE = Object.freeze({
  uz: 'TEKSHIRUV',
  ru: 'ПРОВЕРКА',
});
