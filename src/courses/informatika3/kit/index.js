// ============================================================================
// informatika3/kit/index.js — ЕДИНСТВЕННЫЙ ПУБЛИЧНЫЙ ВХОД В КАРКАС ИНФОРМАТИКИ
//
// Уроки, экраны и сцены информатики импортируют ТОЛЬКО отсюда.
//
// ПОЧЕМУ ЗДЕСЬ РЕЭКСПОРТ, А НЕ СВОЙ ДВИЖОК
//
// Движок урока (AudioEngine, контракт TTS v5.2, ворота «сначала слушаем — потом
// отвечаем», мобильный зум 390px, локализация без тихой подмены языка, раскладка
// вариантов ответа) не зависит от предмета: он про то, как ребёнок проходит урок,
// а не про то, чему урок учит. Копировать его в новый предмет означало бы завести
// ВТОРУЮ копию самой дорогой в отладке части проекта — ровно та ошибка, из-за
// которой один баг в математике приходилось чинить в 45 файлах
// (CLAUDE.md §5, src/courses/README.md).
//
// Поэтому граница проведена так:
//   engine, UI-оболочка, персонажи, эффекты  -> берутся из grade3/kit
//   устройства, поток данных, 3D-модель      -> живут здесь
//
// Визуальный язык (стили, форма кнопок, рамки) тоже общий и сознательно:
// ребёнок узнаёт ту же среду, в которой у него идёт математика, и не тратит
// внимание на новый интерфейс. Различает предметы содержание, а не оформление.
//
// ЧТО ЭТО ЗНАЧИТ ПРАКТИЧЕСКИ: правка grade3/kit меняет и информатику. Это плата
// за один экземпляр кода, и она честнее, чем два расходящихся экземпляра.
// Когда предметов станет три, общая часть переедет в src/courses/shared/ —
// но переедет ИЗМЕРЕННОЙ, а не предположенной (src/courses/README.md).
// ============================================================================

import './theme.css';

// --- каркас 3 класса: движок, оболочка, персонажи, эффекты, локализация -------
export * from '../../grade3/kit/index.js';

// --- устройства и части компьютера -------------------------------------------
export { DeviceIcon, DeviceCard, DEVICE_KEYS, ROLE_COLOR } from './devices.jsx';

// --- поток данных: ввод, обработка, вывод, память ----------------------------
export { FlowArrow, IOChain, SignalFlow, DeviceRow, CommandBoard } from './ioviz.jsx';
