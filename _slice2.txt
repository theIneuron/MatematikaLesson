een = ({ screen, idx, totalScreens, screenMeta, screenContent, question, options, correctIdx, storedAnswer, onAnswer, onNext, onPrev }) => {\n  const lang = useLang();\n  const t = useT();\n  const c = screenContent;\n\n  const audio = useAudio([{\n    id: `s${idx}_intro`,\n    text: c.audio.intro[lang],\n    trigger: 'on_mount',\n    waits_for: { type: 'option_picked' }\n  }]);\n\n  const [picked, setPicked] = useState(storedAnswer?.studentAnswerIndex ?? null);\n  const [revealed, setRevealed] = useState(storedAnswer !== undefined);\n\n  const pick = (i) => {\n    if (revealed) return;\n    setPicked(i);\n    setRevealed(true);\n    const isCorrect = i === correctIdx;\n    onAnswer({\n      stage: screenMeta?.scope ?? null,\n      screenIdx: idx,\n      question: typeof question === 'string' ? question : null,\n      options: options.map(o => typeof o === 'string' ? o : null),\n      correctIndex: correctIdx,\n      correctAnswer: typeof options[correctIdx] === 'string' ? options[correctIdx] : null,\n      studentAnswerIndex: i,\n      studentAnswer: typeof options[i] === 'string' ? options[i] : null,\n      correct: isCorrect\n    });\n    audio.triggerEvent('option_picked');\n\n    if (!audio.muted) {\n      setTimeout(() => {\n        const engine = getAudioEngine();\n        if (engine && !audio.muted) {\n          const fbAudio = isCorrect ? c.audio.on_correct[lang] : c.audio.on_wrong[lang];\n          engine.pushOneOff(fbAudio);\n        }\n      }, 300);\n    }\n  };\n\n  const navContent = (\n    <>\n      <NavBack onPrev={onPrev} label={<BackLabel/>}/>\n      <NavNext disabled={!revealed} onClick={onNext} label={<NextLabel/>}/>\n    </>\n  );\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 24px)' }}>\n        <div className=\"fade-up\">{question}</div>\n        <div className=\"fade-up delay-1\" style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>\n          {options.map((opt, i) => {\n            let cls = 'option';\n            if (revealed) {\n              if (i === correctIdx) cls += ' option-correct';\n              else if (i === picked) cls += ' option-picked-wrong';\n              else cls += ' option-wrong';\n            }\n            return (\n              <button key={i} className={cls} disabled={revealed} onClick={() => pick(i)}\n                style={{ padding: 'clamp(14px, 2vw, 15px) clamp(16px, 2.5vw, 19px)', fontSize: 'clamp(15px, 1.9vw, 15px)', display: 'flex', alignItems: 'center', gap: 12 }}>\n                <span className=\"mono small\" style={{ minWidth: 20, color: revealed && i === correctIdx ? T.success : T.ink3 }}>\n                  {String.fromCharCode(65 + i)}\n                </span>\n                <span style={{ flex: 1 }}>{opt}</span>\n              </button>\n            );\n          })}\n        </div>\n        <FeedbackBlock show={revealed} isCorrect={picked === correctIdx}>\n          <p className=\"small mono\" style={{ margin: 0, marginBottom: 8, fontWeight: 600, color: picked === correctIdx ? T.success : T.accent, textTransform: 'uppercase', letterSpacing: '0.08em' }}>\n            {picked === correctIdx ? (lang === 'uz' ? \"To'g'ri\" : 'Верно') : (lang === 'uz' ? \"Noto'g'ri\" : 'Не совсем')}\n          </p>\n          <p className=\"body\" style={{ margin: 0 }}>\n            {picked === correctIdx\n              ? t(c.correct_text)\n              : t(c[`wrong_${picked}`] || c.wrong_default)}\n          </p>\n        </FeedbackBlock>\n      </div>\n    </Stage>\n  );\n};\n\n// ============================================================\n// SCREEN-КОМПОНЕНТЫ\n// ВАЖНО: контентные SVG-полоски (Screen 1, Screen 2) переведены\n// на новый стиль — без обводки, на нейтральной тени, сегменты\n// разделяются gap-зазорами в bg-цвете (а не borderRight).\n// ============================================================\n\n// ===== SCREEN 0: HOOK =====\nconst Screen0 = ({ screen, totalScreens, storedAnswer, onAnswer, onNext }) => {\n  const c = CONTENT.s0;\n  const t = useT();\n  const audio = useAudio([{ id: 'a', text: c.audio[useLang()], trigger: 'on_mount', waits_for: { type: 'option_picked' } }]);\n  const [picked, setPicked] = useState(null);\n  const pick = (v) => {\n    setPicked(v);\n    onAnswer({ stage: null, screenIdx: 0, studentAnswer: v, correct: true });\n    audio.triggerEvent('option_picked');\n    setTimeout(onNext, 300);\n  };\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 27px)' }}>\n        <h1 className=\"title h-title fade-up\">\n          {t(c.title_part1)} <span className=\"italic\" style={{ color: T.accent }}>{t(c.title_part2_em)}</span> {t(c.title_part3)}<br/>\n          {t(c.title_part4)} <span className=\"italic\" style={{ color: T.accent }}>{t(c.title_part5_em)}</span>{t(c.title_part6)}\n        </h1>\n        <p className=\"body fade-up delay-1\" style={{ color: T.ink2, marginBottom: 'calc(-1 * clamp(20px, 3vw, 27px) + 8px)' }}>{t(c.body)}</p>\n        <div className=\"frame fade-up delay-2\" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 'clamp(8px, 1.5vw, 14px)', flexWrap: 'wrap' }}>\n          <Frac n=\"1\" d=\"2\" size=\"display\"/>\n          <Op size=\"big\">+</Op>\n          <Frac n=\"1\" d=\"3\" size=\"display\"/>\n          <Op size=\"big\">=</Op>\n          <Frac n=\"2\" d=\"5\" size=\"display\" color={T.accent}/>\n        </div>\n        <p className=\"h-sub title fade-up delay-3\">{t(c.question)}</p>\n        <div className=\"fade-up delay-4\" style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>\n          {[{ id: 'yes', label: c.opt_yes }, { id: 'no', label: c.opt_no }, { id: 'idk', label: c.opt_idk }].map(opt => (\n            <button key={opt.id} className=\"option\" disabled={picked !== null} onClick={() => pick(opt.id)}\n              style={{ padding: 'clamp(14px, 2vw, 15px) clamp(16px, 2.5vw, 20px)', fontSize: 'clamp(15px, 1.9vw, 15px)' }}>\n              {t(opt.label)}\n            </button>\n          ))}\n        </div>\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 1: РАЗБОР =====\n// Контентные полоски-диаграммы — новый стиль: тень, сегменты через gap\nconst Screen1 = ({ screen, totalScreens, onNext, onPrev }) => {\n  const c = CONTENT.s1;\n  const t = useT();\n  const lang = useLang();\n  const audioSegs = c.audio[lang].map((text, i) => ({\n    id: `s1_a${i}`,\n    text,\n    trigger: i === 0 ? 'on_mount' : (i === 1 ? 'after_previous' : `on_event:step_${i - 1}`),\n    waits_for: { type: 'button_click', target: i < c.audio[lang].length - 1 ? 'step' : 'next' }\n  }));\n  const audio = useAudio(audioSegs);\n  const [step, setStep] = useState(0);\n\n  const handleStep = () => {\n    if (step < 3) {\n      const nextStep = step + 1;\n      setStep(nextStep);\n      audio.triggerInternal(`step_${nextStep}`);\n    } else {\n      audio.triggerEvent('button_click', 'next');\n      onNext();\n    }\n  };\n\n  const navContent = (\n    <>\n      <NavBack onPrev={onPrev} label={<BackLabel/>}/>\n      <NavNext label={step < 3 ? t(c.btn_next_step) : t(c.btn_final)} onClick={handleStep}/>\n    </>\n  );\n\n  // Контейнер полоски — на тени без обводки (новый стиль)\n  const barContainer = {\n    height: 'clamp(36px, 5vw, 37px)',\n    display: 'flex',\n    borderRadius: 8,\n    overflow: 'hidden',\n    background: T.paper,\n    boxShadow: `0 3px 9px -4px rgba(${T.shadowBase}, 0.16), 0 1px 3px -1px rgba(${T.shadowBase}, 0.10)`\n  };\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 24px)' }}>\n        <h2 className=\"title h-title fade-up\">\n          {t(c.title_part1)}<br/>{t(c.title_part2)} <span className=\"italic\" style={{ color: T.accent }}>{t(c.title_part3_em)}</span>\n        </h2>\n        <p className=\"body fade-up delay-1\" style={{ color: T.ink2 }}>{t(c.intro)}</p>\n\n        <div className=\"fade-up delay-2\">\n          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>\n            <span className=\"mono small\" style={{ color: T.ink2 }}>{t(c.bar1_label)} <Frac n=\"1\" d=\"2\"/></span>\n            <span className=\"mono small\" style={{ color: T.ink3 }}>{t(c.bar1_hint)}</span>\n          </div>\n          <div style={barContainer}>\n            <div style={{ width: '50%', background: T.ink }}/>\n            <div style={{ width: '50%' }}/>\n          </div>\n        </div>\n\n        {step >= 1 && (\n          <div className=\"fade-up\">\n            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>\n              <span className=\"mono small\" style={{ color: T.ink2 }}>{t(c.bar2_label)}</span>\n              <span className=\"mono small\" style={{ color: T.success, fontWeight: 600 }}>{t(c.bar2_hint)}</span>\n            </div>\n            <div style={barContainer}>\n              <div style={{ width: '50%', background: T.ink }}/>\n              <div style={{ width: '33.33%', background: T.accent }}/>\n              <div style={{ flex: 1 }}/>\n            </div>\n          </div>\n        )}\n\n        {step >= 2 && (\n          <div className=\"fade-up\">\n            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>\n              <span className=\"mono small\" style={{ color: T.ink2 }}>{t(c.bar3_label)} <Frac n=\"2\" d=\"5\" color={T.accent}/></span>\n              <span className=\"mono small\" style={{ color: T.ink3 }}>{t(c.bar3_hint)}</span>\n            </div>\n            <div style={barContainer}>\n              <div style={{ width: '40%', background: `repeating-linear-gradient(45deg, ${T.ink3}, ${T.ink3} 4px, transparent 4px, transparent 8px)` }}/>\n              <div style={{ flex: 1 }}/>\n            </div>\n          </div>\n        )}\n\n        {step >= 3 && (\n          <div className=\"fade-up frame-soft\">\n            <p className=\"body\" style={{ fontWeight: 600, margin: 0, marginBottom: 6 }}>\n              <Frac n=\"2\" d=\"5\"/>{t(c.conclusion_strong)}\n            </p>\n            <p className=\"body\" style={{ margin: 0, color: T.ink2 }}>\n              {t(c.conclusion_rest_p1)} <b style={{ color: T.ink }}>{t(c.conclusion_emphasis)}</b>{t(c.conclusion_rest_p2)} <Frac n=\"2\" d=\"5\" color={T.accent}/> {t(c.conclusion_rest_p3)}\n            </p>\n          </div>\n        )}\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 2: ПОЛЗУНОК =====\n// Полоски с сегментами — новый стиль: gap 2px в bg-цвете вместо borderRight\n// Кнопка «Проверить» — справа (как «Дальше»), btn-white-accent\nconst Screen2 = ({ screen, totalScreens, onNext, onPrev }) => {\n  const c = CONTENT.s2;\n  const t = useT();\n  const lang = useLang();\n  const audio = useAudio([{ id: 'a', text: c.audio[lang], trigger: 'on_mount', waits_for: { type: 'check_pressed' } }]);\n  const [n, setN] = useState(2);\n  const [checked, setChecked] = useState(false);\n  const feedbackRef = useRef(null);\n  const fitsMadina = n % 2 === 0;\n  const fitsZaynab = n % 3 === 0;\n  const fitsBoth = fitsMadina && fitsZaynab;\n\n  const getFeedback = () => {\n    if (fitsBoth) {\n      return {\n        type: 'success',\n        title: c.fb_success_title,\n        visual: <><span style={{ color: T.success, fontWeight: 600 }}>{n}</span> {t(c.fb_success_part1)} <Frac n=\"1\" d={String(n)}/>{t(c.fb_success_part2)}</>,\n        audio: lang === 'uz' ? `${n} ham 2 ga, ham 3 ga bo'linadi. Ikkala kasrni ${n} ga ko'ra yozish mumkin.` : `${n} делится и на два, и на три. Обе дроби можно записать в долях по одной ${n === 6 ? 'шестой' : n + 'й'}.`\n      };\n    }\n    if (fitsMadina && !fitsZaynab) {\n      return { type: 'wrong', title: c.fb_wrong_title, visual: <>{n} {t(c.fb_wrong_masha_yes)}</>,\n               audio: lang === 'uz' ? `Madinaga ${n} mos, lekin Zaynab bu songa tekis bo'linmaydi.` : `Мадине ${n} подходит, но Зайнаб на ${n} ровно не делится.` };\n    }\n    if (!fitsMadina && fitsZaynab) {\n      return { type: 'wrong', title: c.fb_wrong_title, visual: <>{n} {t(c.fb_wrong_anya_yes)}</>,\n               audio: lang === 'uz' ? `Zaynabga ${n} mos, lekin Madina bu songa tekis bo'linmaydi.` : `Зайнаб ${n} подходит, но Мадина на ${n} ровно не делится.` };\n    }\n    return { type: 'wrong', title: c.fb_wrong_title, visual: <>{n} {t(c.fb_wrong_neither)}</>,\n             audio: lang === 'uz' ? `${n} na 2 ga, na 3 ga tekis bo'linmaydi.` : `Число ${n} не делится ровно ни на два, ни на три.` };\n  };\n\n  const handleCheck = () => {\n    const fb = getFeedback();\n    setChecked(true);\n    audio.triggerEvent('check_pressed');\n    if (!audio.muted) {\n      setTimeout(() => {\n        const engine = getAudioEngine();\n        if (engine && !audio.muted) {\n          engine.pushOneOff(fb.audio);\n        }\n      }, 300);\n    }\n  };\n\n  const handleSliderChange = (newN) => { setN(newN); if (checked) setChecked(false); };\n  const feedback = checked ? getFeedback() : null;\n\n  useEffect(() => {\n    if (checked && feedbackRef.current) {\n      // block: 'end' — гарантирует прокрутку даже если feedback частично\n      // виден. timeout 200 — даёт время анимации feedback-block (0.4s\n      // expand) хотя бы начаться, чтобы scroll учитывал реальную высоту.\n      setTimeout(() => {\n        if (feedbackRef.current) {\n          feedbackRef.current.scrollIntoView({ behavior: 'smooth', block: 'end' });\n        }\n      }, 200);\n    }\n  }, [checked]);\n\n  const navContent = (\n    <>\n      <NavBack onPrev={onPrev} label={<BackLabel/>}/>\n      <NavNext disabled={!fitsBoth || !checked} label={fitsBoth && checked ? <NextLabel/> : t(c.btn_disabled_label)} onClick={onNext}/>\n    </>\n  );\n\n  // Контейнер полоски-сегментов: фон bg, чтобы зазоры между сегментами\n  // (через gap 2px) читались как тонкие светлые полосы.\n  const barSegments = {\n    height: 'clamp(36px, 5vw, 37px)',\n    display: 'flex',\n    gap: 2,\n    borderRadius: 8,\n    overflow: 'hidden',\n    background: T.bg,\n    boxShadow: `0 3px 9px -4px rgba(${T.shadowBase}, 0.16), 0 1px 3px -1px rgba(${T.shadowBase}, 0.10)`\n  };\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 24px)' }}>\n        <h2 className=\"title h-title fade-up\">\n          {t(c.title_part1)}<br/>{t(c.title_part2)} <span className=\"italic\" style={{ color: T.accent }}>{t(c.title_part3_em)}</span>{t(c.title_part4)}\n        </h2>\n\n        <div className=\"fade-up delay-1\">\n          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>\n            <span className=\"mono small\" style={{ color: T.ink2 }}>{t(c.masha_prefix)} <Frac n=\"1\" d=\"2\"/> {t(c.delim_text)} {n}</span>\n            <span className=\"mono small\" style={{ color: fitsMadina ? T.success : T.ink3, fontWeight: 600 }}>\n              {fitsMadina ? `= ${n/2}/${n}` : t(c.not_fits_text)}\n            </span>\n          </div>\n          <div style={barSegments}>\n            {[...Array(n)].map((_, i) => (\n              <div key={i} style={{ flex: 1, background: fitsMadina && i < n/2 ? T.ink : T.paper, transition: 'background 0.3s' }}/>\n            ))}\n          </div>\n        </div>\n\n        <div className=\"fade-up delay-2\">\n          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>\n            <span className=\"mono small\" style={{ color: T.ink2 }}>{t(c.anya_prefix)} <Frac n=\"1\" d=\"3\"/> {t(c.delim_text)} {n}</span>\n            <span className=\"mono small\" style={{ color: fitsZaynab ? T.success : T.ink3, fontWeight: 600 }}>\n              {fitsZaynab ? `= ${n/3}/${n}` : t(c.not_fits_text)}\n            </span>\n          </div>\n          <div style={barSegments}>\n            {[...Array(n)].map((_, i) => (\n              <div key={i} style={{ flex: 1, background: fitsZaynab && i < n/3 ? T.accent : T.paper, transition: 'background 0.3s' }}/>\n            ))}\n          </div>\n        </div>\n\n        <div className=\"fade-up delay-3\">\n          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline' }}>\n            <span className=\"eyebrow\" style={{ color: T.ink2 }}>{t(c.eyebrow_slider)}</span>\n            <span className=\"display\" style={{ fontSize: 'clamp(32px, 5.5vw, 44px)', color: fitsBoth && checked ? T.success : T.ink, transition: 'color 0.3s' }}>{n}</span>\n          </div>\n          <Slider value={n} min={2} max={12} onChange={handleSliderChange}/>\n          <div className=\"mono small\" style={{ display: 'flex', justifyContent: 'space-between', color: T.ink3 }}><span>2</span><span>12</span></div>\n        </div>\n\n        {/* Кнопка «Проверить» — справа, как «Дальше», btn-white-accent */}\n        <div className=\"fade-up delay-4\" style={{ display: 'flex', justifyContent: 'flex-end' }}>\n          <button className=\"btn-white-accent\" disabled={checked} onClick={handleCheck}>\n            {t(c.btn_check)}\n          </button>\n        </div>\n\n        {feedback && (\n          <div ref={feedbackRef} className={feedback.type === 'success' ? 'frame-success' : 'frame-soft'}>\n            <p className=\"small mono\" style={{ margin: 0, marginBottom: 6, fontWeight: 600, color: feedback.type === 'success' ? T.success : T.accent, textTransform: 'uppercase', letterSpacing: '0.08em' }}>\n              {t(feedback.title)}\n            </p>\n            <p className=\"body\" style={{ margin: 0 }}>{feedback.visual}</p>\n          </div>\n        )}\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 3: ПОНЯТИЕ =====\nconst Screen3 = ({ screen, totalScreens, onNext, onPrev }) => {\n  const c = CONTENT.s3;\n  const t = useT();\n  const lang = useLang();\n  const audio = useAudio([{ id: 'a', text: c.audio[lang], trigger: 'on_mount', waits_for: { type: 'button_click', target: 'next' } }]);\n  const handleNext = () => { audio.triggerEvent('button_click', 'next'); onNext(); };\n  const navContent = (<><NavBack onPrev={onPrev} label={<BackLabel/>}/><NavNext onClick={handleNext} label={<NextLabel/>}/></>);\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 27px)' }}>\n        <div className=\"fade-up\">\n          <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n          <h2 className=\"title h-title\" style={{ marginTop: 10 }}>\n            {t(c.title_part1)}<br/><span className=\"italic\">{t(c.title_part2_em)}</span>\n          </h2>\n        </div>\n        <div className=\"frame fade-up delay-1\" style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-around', gap: 'clamp(16px, 3vw, 27px)', flexWrap: 'wrap' }}>\n          <div style={{ textAlign: 'center' }}>\n            <Frac n=\"1\" d={<span style={{ color: T.accent }}>2</span>} size=\"mid\"/>\n            <p className=\"mono small\" style={{ marginTop: 8, color: T.ink2 }}>{t(c.card_hint1)}</p>\n          </div>\n          <div style={{ textAlign: 'center' }}>\n            <Frac n=\"1\" d={<span style={{ color: T.accent }}>3</span>} size=\"mid\"/>\n            <p className=\"mono small\" style={{ marginTop: 8, color: T.ink2 }}>{t(c.card_hint2)}</p>\n          </div>\n          <Op size=\"big\">→</Op>\n          <div style={{ textAlign: 'center' }}>\n            <div className=\"display\" style={{ fontSize: 'clamp(45px, 8vw, 65px)', color: T.accent }}>6</div>\n            <p className=\"mono small\" style={{ marginTop: 8, color: T.accent, fontWeight: 600 }}>{t(c.card_label)}</p>\n          </div>\n        </div>\n        <p className=\"body fade-up delay-2\" style={{ color: T.ink2 }}>{t(c.outro)}</p>\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 4: ТРЕНИРОВКА 1 (QuestionScreen) =====\nconst Screen4 = (props) => {\n  const c = CONTENT.s4;\n  const t = useT();\n  const lang = useLang();\n  return (\n    <QuestionScreen\n      {...props} idx={4} screenMeta={SCREEN_META[4]} screenContent={c}\n      question={<>\n        <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n        <h2 className=\"title h-sub\" style={{ marginTop: 8 }}>\n          {lang === 'uz' ? (\n            <><Frac n=\"1\" d=\"4\" size=\"mid\"/> {t(c.title_conj)} <Frac n=\"1\" d=\"6\" size=\"mid\"/> {t(c.title_suffix)}</>\n          ) : (\n            <>{t(c.title_prefix)} <Frac n=\"1\" d=\"4\" size=\"mid\"/> {t(c.title_conj)} <Frac n=\"1\" d=\"6\" size=\"mid\"/></>\n          )}\n        </h2>\n      </>}\n      options={[t(c.opt0), t(c.opt1), t(c.opt2), t(c.opt3)]}\n      correctIdx={1}\n    />\n  );\n};\n\n// ===== SCREEN 5: ПРАВИЛО =====\nconst Screen5 = ({ screen, totalScreens, onNext, onPrev }) => {\n  const c = CONTENT.s5;\n  const t = useT();\n  const lang = useLang();\n  const audio = useAudio([{ id: 'a', text: c.audio[lang], trigger: 'on_mount', waits_for: { type: 'button_click', target: 'next' } }]);\n  const handleNext = () => { audio.triggerEvent('button_click', 'next'); onNext(); };\n  const navContent = (<><NavBack onPrev={onPrev} label={<BackLabel/>}/><NavNext onClick={handleNext} label={<NextLabel/>}/></>);\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 27px)' }}>\n        <div className=\"fade-up\">\n          <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n          <h2 className=\"title h-title\" style={{ marginTop: 10 }}>\n            {t(c.title_part1)}<br/><span className=\"italic\">{t(c.title_part2_em)}</span>\n          </h2>\n        </div>\n        <div className=\"frame fade-up delay-1\">\n          <p className=\"body\" style={{ margin: 0, marginBottom: 16, color: T.ink2 }}>\n            {t(c.example_intro_p1)} <Frac n=\"1\" d=\"2\"/> {t(c.example_intro_p2)} <Frac n=\"?\" d=\"6\"/>{t(c.example_intro_p3)}\n          </p>\n          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 'clamp(6px, 1.5vw, 12px)', flexWrap: 'wrap', padding: 'clamp(8px, 2vw, 14px) 0' }}>\n            <Frac n=\"1\" d=\"2\" size=\"mid\"/>\n            <Op size=\"mid\">=</Op>\n            <Frac n={<span>1<span className=\"mop\" style={{ fontSize: '0.8em', padding: '0 0.1em' }}>×</span>3</span>} d={<span>2<span className=\"mop\" style={{ fontSize: '0.8em', padding: '0 0.1em' }}>×</span>3</span>} size=\"mid\"/>\n            <Op size=\"mid\">=</Op>\n            <Frac n=\"3\" d=\"6\" size=\"mid\" color={T.accent}/>\n          </div>\n        </div>\n        <p className=\"body fade-up delay-2\" style={{ color: T.ink2 }}>{t(c.outro)}</p>\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 6: ВВОД ЧИСЛА =====\n// Кнопка «Проверить» — справа, btn-white-accent\nconst Screen6 = ({ screen, totalScreens, storedAnswer, onAnswer, onNext, onPrev }) => {\n  const c = CONTENT.s6;\n  const t = useT();\n  const lang = useLang();\n  const audio = useAudio([{ id: 'a', text: c.audio.intro[lang], trigger: 'on_mount', waits_for: { type: 'check_pressed' } }]);\n  const [value, setValue] = useState(storedAnswer?.studentAnswer ?? '');\n  const [revealed, setRevealed] = useState(storedAnswer !== undefined);\n  const correct = 4;\n\n  const submit = () => {\n    const v = parseInt(value);\n    if (isNaN(v)) return;\n    const isCorrect = v === correct;\n    setRevealed(true);\n    onAnswer({\n      stage: SCREEN_META[6].scope,\n      screenIdx: 6,\n      question: '1/3 = ?/12',\n      correctAnswer: '4',\n      studentAnswer: String(v),\n      correct: isCorrect\n    });\n    audio.triggerEvent('check_pressed');\n    if (!audio.muted) {\n      const fbText = isCorrect ? c.audio.on_correct[lang] : c.audio.on_wrong[lang];\n      setTimeout(() => {\n        const engine = getAudioEngine();\n        if (engine && !audio.muted) {\n          engine.pushOneOff(fbText);\n        }\n      }, 300);\n    }\n  };\n\n  const isCorrect = parseInt(value) === correct;\n  const navContent = (<><NavBack onPrev={onPrev} label={<BackLabel/>}/><NavNext disabled={!isCorrect || !revealed} onClick={onNext} label={<NextLabel/>}/></>);\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 24px)' }}>\n        <div className=\"fade-up\">\n          <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n          <h2 className=\"title h-sub\" style={{ marginTop: 8 }}>\n            {lang === 'uz' ? (\n              <><Frac n=\"1\" d=\"3\" size=\"mid\"/> {t(c.title_uz_eq)} <Frac n=\"?\" d=\"12\" size=\"mid\"/> {t(c.title_uz_suffix)}</>\n            ) : (\n              <>{t(c.title_ru_part1)}<br/>{t(c.title_ru_part2)} <Frac n=\"1\" d=\"3\" size=\"mid\"/> {t(c.title_ru_part3)} <Frac n=\"?\" d=\"12\" size=\"mid\"/>{t(c.title_ru_part4)}</>\n            )}\n          </h2>\n        </div>\n        <div className=\"frame fade-up delay-1\" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 'clamp(8px, 1.5vw, 12px)', flexWrap: 'wrap' }}>\n          <Frac n=\"1\" d=\"3\" size=\"mid\"/>\n          <Op size=\"mid\">=</Op>\n          <span className=\"frac frac-mid\">\n            <input type=\"number\" className={`answer-input ${revealed ? (isCorrect ? 'correct' : 'wrong') : ''}`}\n              value={value} onChange={e => { setValue(e.target.value); if (revealed) setRevealed(false); }}\n              disabled={revealed && isCorrect} onKeyDown={e => e.key === 'Enter' && submit()} style={{ marginBottom: 4 }}/>\n            <span className=\"bar\" style={{ width: 'clamp(64px, 10vw, 75px)' }}/>\n            <span className=\"d\" style={{ marginTop: 4 }}>12</span>\n          </span>\n        </div>\n        {/* Кнопка «Проверить» — справа, btn-white-accent */}\n        <div className=\"fade-up delay-2\" style={{ display: 'flex', justifyContent: 'flex-end' }}>\n          <button className=\"btn-white-accent\" disabled={!value || (revealed && isCorrect)} onClick={submit}>\n            {t(c.btn_check)}\n          </button>\n        </div>\n        <FeedbackBlock show={revealed} isCorrect={isCorrect}>\n          <p className=\"small mono\" style={{ margin: 0, marginBottom: 8, fontWeight: 600, color: isCorrect ? T.success : T.accent, textTransform: 'uppercase', letterSpacing: '0.08em' }}>\n            {isCorrect ? t(c.fb_correct_title) : t(c.fb_wrong_title)}\n          </p>\n          <p className=\"body\" style={{ margin: 0 }}>{isCorrect ? t(c.fb_correct) : t(c.fb_wrong)}</p>\n        </FeedbackBlock>\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 7: ПОЛНЫЙ РАЗБОР =====\nconst Screen7 = ({ screen, totalScreens, onNext, onPrev }) => {\n  const c = CONTENT.s7;\n  const t = useT();\n  const lang = useLang();\n  const audio = useAudio([{ id: 'a', text: c.audio[lang], trigger: 'on_mount', waits_for: { type: 'button_click', target: 'next' } }]);\n  const handleNext = () => { audio.triggerEvent('button_click', 'next'); onNext(); };\n  const navContent = (<><NavBack onPrev={onPrev} label={<BackLabel/>}/><NavNext onClick={handleNext} label={<NextLabel/>}/></>);\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 20px)' }}>\n        <h2 className=\"title h-title fade-up\">\n          {t(c.title_part1)} <span className=\"italic\">{t(c.title_part2_em)}</span>\n        </h2>\n        <p className=\"body fade-up delay-1\" style={{ color: T.ink2 }}>\n          {lang === 'uz' ? (\n            <><Frac n=\"1\" d=\"2\"/> {t(c.intro_plus)} <Frac n=\"1\" d=\"3\"/> {t(c.intro_uz_suffix)}</>\n          ) : (\n            <>{t(c.intro_ru_prefix)} <Frac n=\"1\" d=\"2\"/> {t(c.intro_plus)} <Frac n=\"1\" d=\"3\"/> {t(c.intro_ru_suffix)}</>\n          )}\n        </p>\n        <div className=\"frame fade-up delay-2\" style={{ display: 'flex', flexDirection: 'column', gap: 18 }}>\n          {[\n            { num: '01', title: c.step1_title, math: <>2 <Op size=\"sm\">×</Op> 3 <Op size=\"sm\">=</Op> <span style={{ color: T.accent, fontFamily: \"'Fraunces', serif\", fontSize: '1.3em' }}>6</span></> },\n            { num: '02', title: c.step2_title, math: <><Frac n=\"1\" d=\"2\"/> <Op size=\"sm\">=</Op> <Frac n={<span>1<Op size=\"sm\">×</Op>3</span>} d={<span>2<Op size=\"sm\">×</Op>3</span>}/> <Op size=\"sm\">=</Op> <Frac n=\"3\" d=\"6\" color={T.accent}/></> },\n            { num: '03', title: c.step3_title, math: <><Frac n=\"1\" d=\"3\"/> <Op size=\"sm\">=</Op> <Frac n={<span>1<Op size=\"sm\">×</Op>2</span>} d={<span>3<Op size=\"sm\">×</Op>2</span>}/> <Op size=\"sm\">=</Op> <Frac n=\"2\" d=\"6\" color={T.accent}/></> },\n            { num: '04', title: c.step4_title, math: <><Frac n=\"3\" d=\"6\"/> <Op size=\"sm\">+</Op> <Frac n=\"2\" d=\"6\"/> <Op size=\"sm\">=</Op> <Frac n=\"5\" d=\"6\" color={T.success}/></> }\n          ].map((s, i) => (\n            <div key={i} style={{ display: 'grid', gridTemplateColumns: '44px 1fr', gap: 16, alignItems: 'center' }}>\n              <div className=\"mono small\" style={{ color: T.accent, fontWeight: 600 }}>{s.num}</div>\n              <div>\n                <div className=\"small\" style={{ color: T.ink2, marginBottom: 6, fontWeight: 500 }}>{t(s.title)}</div>\n                <div style={{ fontSize: 'clamp(16px, 2.5vw, 19px)', display: 'flex', alignItems: 'center', flexWrap: 'wrap', gap: 6 }}>{s.math}</div>\n              </div>\n            </div>\n          ))}\n        </div>\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 8: ТРЕНИРОВКА 4/4 =====\nconst Screen8 = (props) => {\n  const c = CONTENT.s8;\n  const t = useT();\n  const lang = useLang();\n  return (\n    <QuestionScreen {...props} idx={8} screenMeta={SCREEN_META[8]} screenContent={c}\n      question={<>\n        <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n        <h2 className=\"title h-sub\" style={{ marginTop: 8 }}>\n          {lang === 'uz' ? (\n            <><Frac n=\"1\" d=\"4\" size=\"mid\"/> <Op size=\"mid\">{t(c.title_plus)}</Op> <Frac n=\"1\" d=\"6\" size=\"mid\"/> {t(c.title_uz_suffix)}</>\n          ) : (\n            <>{t(c.title_ru_part1)} <Frac n=\"1\" d=\"4\" size=\"mid\"/> <Op size=\"mid\">{t(c.title_plus)}</Op> <Frac n=\"1\" d=\"6\" size=\"mid\"/>{t(c.title_q)}</>\n          )}\n        </h2>\n      </>}\n      options={[t(c.opt0), t(c.opt1), t(c.opt2), t(c.opt3)]}\n      correctIdx={3}\n    />\n  );\n};\n\n// ===== SCREEN 9: КЕЙС ЗАЙНАБ =====\nconst Screen9 = ({ screen, totalScreens, onNext, onPrev }) => {\n  const c = CONTENT.s9;\n  const t = useT();\n  const lang = useLang();\n  const audio = useAudio([{ id: 'a', text: c.audio[lang], trigger: 'on_mount', waits_for: { type: 'button_click', target: 'next' } }]);\n  const handleNext = () => { audio.triggerEvent('button_click', 'next'); onNext(); };\n  const navContent = (<><NavBack onPrev={onPrev} label={<BackLabel/>}/><NavNext label={t(c.btn_help)} onClick={handleNext}/></>);\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 'clamp(20px, 3vw, 24px)' }}>\n        <h2 className=\"title h-title fade-up\">\n          <span className=\"italic\">{t(c.name_em)}</span> {t(c.title_rest)}\n        </h2>\n        <div className=\"frame fade-up delay-1\">\n          <p className=\"body\" style={{ margin: 0, color: T.ink }}>\n            {t(c.body_p1_part1)} <Frac n=\"1\" d=\"2\"/> {t(c.body_p1_part2)} <Frac n=\"1\" d=\"4\"/> {t(c.body_p1_part3)}\n          </p>\n          <p className=\"body\" style={{ margin: '12px 0 0', color: T.ink2 }}>{t(c.body_p2)}</p>\n        </div>\n        <div className=\"fade-up delay-2\" style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(120px, 1fr))', gap: 12 }}>\n          {[\n            { label: c.card_stakan_label, value: t(c.card_stakan_value) },\n            { label: c.card_muka_label, value: <Frac n=\"1\" d=\"2\"/> },\n            { label: c.card_kakao_label, value: <Frac n=\"1\" d=\"4\"/> }\n          ].map((it, i) => (\n            <div key={i} className=\"frame\" style={{ padding: 'clamp(12px, 2vw, 14px)', textAlign: 'center' }}>\n              <p className=\"eyebrow\" style={{ margin: 0, color: T.ink3, fontSize: 10 }}>{t(it.label)}</p>\n              <div style={{ marginTop: 8, fontSize: 'clamp(18px, 3vw, 20px)', fontFamily: \"'Fraunces', serif\" }}>{it.value}</div>\n            </div>\n          ))}\n        </div>\n        <p className=\"body fade-up delay-3\" style={{ color: T.ink2 }}>\n          {t(c.outro_part1)} <Frac n=\"1\" d=\"2\"/> <Op size=\"sm\">{t(c.outro_part2)}</Op> <Frac n=\"1\" d=\"4\"/>{t(c.outro_part3)}\n        </p>\n      </div>\n    </Stage>\n  );\n};\n\n// ===== SCREEN 10: КЕЙС ШАГ 1 =====\nconst Screen10 = (props) => {\n  const c = CONTENT.s10;\n  const t = useT();\n  return (\n    <QuestionScreen {...props} idx={10} screenMeta={SCREEN_META[10]} screenContent={c}\n      question={<>\n        <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n        <h2 className=\"title h-sub\" style={{ marginTop: 8 }}>\n          {t(c.title_part1)}<br/>{t(c.title_part2)} <Frac n=\"1\" d=\"2\" size=\"mid\"/> {t(c.title_conj)} <Frac n=\"1\" d=\"4\" size=\"mid\"/>\n        </h2>\n      </>}\n      options={[t(c.opt0), t(c.opt1), t(c.opt2), t(c.opt3)]}\n      correctIdx={1}\n    />\n  );\n};\n\n// ===== SCREEN 11: КЕЙС ШАГ 2 =====\nconst Screen11 = (props) => {\n  const c = CONTENT.s11;\n  const t = useT();\n  const lang = useLang();\n  return (\n    <QuestionScreen {...props} idx={11} screenMeta={SCREEN_META[11]} screenContent={c}\n      question={<>\n        <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n        <h2 className=\"title h-sub\" style={{ marginTop: 8 }}>\n          {t(c.title_part1)}<br/>\n          {lang === 'uz' ? (\n            <><Frac n=\"1\" d=\"2\" size=\"mid\"/> <Op size=\"mid\">{t(c.title_plus)}</Op> <Frac n=\"1\" d=\"4\" size=\"mid\"/> {t(c.title_uz_suffix)}</>\n          ) : (\n            <>{t(c.title_ru_part2)} <Frac n=\"1\" d=\"2\" size=\"mid\"/> <Op size=\"mid\">{t(c.title_plus)}</Op> <Frac n=\"1\" d=\"4\" size=\"mid\"/>{t(c.title_q)}</>\n          )}\n        </h2>\n      </>}\n      options={[t(c.opt0), t(c.opt1), t(c.opt2), t(c.opt3)]}\n      correctIdx={2}\n    />\n  );\n};\n\n// ===== SCREEN 12: КЕЙС ИТОГ =====\nconst Screen12 = (props) => {\n  const c = CONTENT.s12;\n  const t = useT();\n  const lang = useLang();\n  return (\n    <QuestionScreen {...props} idx={12} screenMeta={SCREEN_META[12]} screenContent={c}\n      question={<>\n        <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n        <h2 className=\"title h-sub\" style={{ marginTop: 8 }}>\n          {lang === 'uz' ? (\n            <>{t(c.title_uz_prefix)} <Frac n=\"3\" d=\"4\" size=\"mid\"/> {t(c.title_uz_suffix)}<br/>{t(c.title_part3)}</>\n          ) : (\n            <>{t(c.title_ru_part1)} <Frac n=\"3\" d=\"4\" size=\"mid\"/>{t(c.title_ru_part2)}<br/>{t(c.title_part3)}</>\n          )}\n        </h2>\n      </>}\n      options={[t(c.opt0), t(c.opt1), t(c.opt2), t(c.opt3)]}\n      correctIdx={1}\n    />\n  );\n};\n\n// ===== SCREEN 13: ФИНАЛЬНАЯ ПРОВЕРКА =====\nconst Screen13 = (props) => {\n  const c = CONTENT.s13;\n  const t = useT();\n  const lang = useLang();\n  return (\n    <QuestionScreen {...props} idx={13} screenMeta={SCREEN_META[13]} screenContent={c}\n      question={<>\n        <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n        <h2 className=\"title h-sub\" style={{ marginTop: 8 }}>\n          {lang === 'uz' ? (\n            <><Frac n=\"1\" d=\"2\" size=\"mid\"/> <Op size=\"mid\">{t(c.title_plus)}</Op> <Frac n=\"1\" d=\"5\" size=\"mid\"/> {t(c.title_uz_suffix)}</>\n          ) : (\n            <>{t(c.title_ru_part1)} <Frac n=\"1\" d=\"2\" size=\"mid\"/> <Op size=\"mid\">{t(c.title_plus)}</Op> <Frac n=\"1\" d=\"5\" size=\"mid\"/>{t(c.title_q)}</>\n          )}\n        </h2>\n      </>}\n      options={[t(c.opt0), t(c.opt1), t(c.opt2), t(c.opt3)]}\n      correctIdx={2}\n    />\n  );\n};\n\n// ===== SCREEN 14: ФИНАЛ =====\nconst Screen14 = ({ screen, totalScreens, answers, onReset, onPrev, finishLesson }) => {\n  const c = CONTENT.s14;\n  const t = useT();\n  const lang = useLang();\n  const audio = useAudio([{ id: 'a', text: c.audio[lang], trigger: 'on_mount', waits_for: null }]);\n  const scored = [4, 6, 8, 10, 11, 12, 13];\n  const correct = scored.filter(i => answers[i]?.correct).length;\n  const total = scored.length;\n\n  // При монтировании финального экрана — собрать payload и отправить LMS\n  useEffect(() => {\n    finishLesson();\n    // eslint-disable-next-line\n  }, []);\n\n  const navContent = (\n    <>\n      <NavBack onPrev={onPrev} label={<BackLabel/>}/>\n      <button className=\"btn-ghost\" onClick={onReset}>{t(c.btn_reset)}</button>\n      <button className=\"btn-white-accent\" disabled style={{ marginLeft: 'auto' }}>{t(c.btn_next)}</button>\n    </>\n  );\n\n  return (\n    <Stage eyebrow={c.eyebrow} screen={screen} totalScreens={totalScreens} navContent={navContent} audioState={audio}>\n      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center', gap: 'clamp(20px, 4vw, 31px)' }}>\n        <div className=\"fade-up\">\n          <p className=\"eyebrow\" style={{ color: T.accent }}>{t(c.label)}</p>\n          <h2 className=\"title h-title\" style={{ marginTop: 10 }}>\n            {t(c.title_part1)}<br/><span className=\"italic\">{t(c.title_part2_em)}</span>\n          </h2>\n        </div>\n        <div className=\"frame fade-up delay-1\" style={{ textAlign: 'center' }}>\n          <p className=\"eyebrow\" style={{ color: T.ink3, margin: 0 }}>{t(c.score_label)}</p>\n          <div className=\"display\" style={{ fontSize: 'clamp(64px, 12vw, 95px)', marginTop: 8 }}>\n            <span style={{ color: correct >= total * 0.7 ? T.success : T.accent }}>{correct}</span>\n            <span style={{ color: T.ink3 }}>/{total}</span>\n          </div>\n          <p className=\"body\" style={{ color: T.ink2, marginTop: 8 }}>\n            {correct >= total * 0.85 && t(c.msg_excellent)}\n            {correct >= total * 0.6 && correct < total * 0.85 && t(c.msg_good)}\n            {correct < total * 0.6 && t(c.msg_repeat)}\n          </p>\n        </div>\n        <div className=\"fade-up delay-2 frame\">\n          <p className=\"eyebrow\" style={{ color: T.accent, margin: 0 }}>{t(c.main_label)}</p>\n          <ul className=\"body\" style={{ marginTop: 12, paddingLeft: 20, color: T.ink2 }}>\n            <li>{t(c.main_1)}</li>\n            <li>{t(c.main_2)}</li>\n            <li>{t(c.main_3)}</li>\n            <li>{t(c.main_4)}</li>\n          </ul>\n        </div>\n      </div>\n    </Stage>\n  );\n};\n\n// ============================================================\n// КОРНЕВОЙ КОМПОНЕНТ\n// Сигнатура из infrastructure_v1: ({ lang, onFinished }).\n// Preview-fallback: если props не пришли (запуск в artifacts) —\n// дефолтим в 'ru' и логируем payload.\n//\n// Для preview методиста добавлен переключатель RU/UZ — в production\n// он не отображается, так как lang приходит как prop из платформы.\n// ============================================================\nexport default function MathLesson_5_06({ lang: langProp, onFinished }) {\n  // Preview-fallback\n  const isPreview = !langProp;\n  const [previewLang, setPreviewLang] = useState('ru');\n  const lang = langProp || previewLang;\n\n  const safeOnFinished = onFinished || ((payload) => {\n    // eslint-disable-next-line no-console\n    console.log('[Preview] onFinished payload:', payload);\n  });\n\n  const [current, setCurrent] = useState(0);\n  const [answers, setAnswers] = useState([]);\n  const startTimeRef = useRef(Date.now());\n\n  const recordAnswer = useCallback((screenIdx, data) => {\n    setAnswers(prev => {\n      const next = [...prev];\n      next[screenIdx] = data;\n      return next;\n    });\n  }, []);\n\n  const reset = useCallback(() => {\n    setAnswers([]);\n    setCurrent(0);\n    startTimeRef.current = Date.now();\n  }, []);\n\n  const finishLesson = useCallback(() => {\n    const scoredScreens = SCREEN_META.filter(s => s.scored);\n    const finalScreens = scoredScreens.filter(s => s.scope === 'final');\n    const correctAnswersCount = answers.filter((a, i) => a && SCREEN_META[i]?.scored && a.correct).length;\n    const finalCorrect = answers.filter((a, i) => a && SCREEN_META[i]?.scope === 'final' && a.correct).length;\n\n    const payload = {\n      lessonId: LESSON_META.lessonId,\n      lessonTitle: LESSON_META.lessonTitle,\n      durationSec: Math.floor((Date.now() - startTimeRef.current) / 1000),\n      totalQuestions: scoredScreens.length,\n      correctAnswers: correctAnswersCount,\n      scorePercent: scoredScreens.length > 0\n        ? Math.round((correctAnswersCount / scoredScreens.length) * 100)\n        : 0,\n      finalScore: finalCorrect,\n      finalTotal: finalScreens.length,\n      passed: finalScreens.length > 0\n        ? finalCorrect / finalScreens.length >= 0.6\n        : (scoredScreens.length > 0 ? (correctAnswersCount / scoredScreens.length) >= 0.6 : false),\n      answers: answers.filter(Boolean)\n    };\n    safeOnFinished(payload);\n  }, [answers, safeOnFinished]);\n\n  const screens = [Screen0, Screen1, Screen2, Screen3, Screen4, Screen5, Screen6, Screen7, Screen8, Screen9, Screen10, Screen11, Screen12, Screen13, Screen14];\n  const CurrentScreen = screens[current];\n\n  const next = () => setCurrent(s => Math.min(s + 1, TOTAL_SCREENS - 1));\n  const prev = () => setCurrent(s => Math.max(s - 1, 0));\n\n  const handleAnswer = useCallback((data) => {\n    recordAnswer(current, data);\n  }, [current, recordAnswer]);\n\n  return (\n    <LangContext.Provider value={lang}>\n      <style>{STYLES}</style>\n      <div className=\"lesson-root\">\n        {isPreview && (\n          <div className=\"lang-switch\">\n            <button className={`lang-btn ${previewLang === 'ru' ? 'active' : ''}`} onClick={() => setPreviewLang('ru')}>RU</button>\n            <button className={`lang-btn ${previewLang === 'uz' ? 'active' : ''}`} onClick={() => setPreviewLang('uz')}>UZ</button>\n          </div>\n        )}\n        <CurrentScreen\n          screen={current}\n          totalScreens={TOTAL_SCREENS}\n          storedAnswer={answers[current]}\n          answers={answers}\n          onAnswer={handleAnswer}\n          onNext={next}\n          onPrev={prev}\n          onReset={reset}\n          finishLesson={finishLesson}\n        />\n      </div>\n    </LangContext.Provider>\n  );\n}\n\n// ============================================================\n// CSS-БЛОК\n// Из infrastructure_v1 (936px, без @import шрифтов, новые отступы),\n// поверх — визуальный язык v15 (тени вместо рамок, white-accent CTA,\n// glow, polosa-исключение).\n//\n// Токены теней (комментарии — для design_system, в production\n// стоит вынести в CSS-переменные):\n//   --shadow-card-rest:  0 3px 9px -4px rgba(58,53,48,.16), 0 1px 3px -1px rgba(58,53,48,.10)\n//   --shadow-card-hover: 0 10px 26px -6px rgba(58,53,48,.22), 0 3px 8px -2px rgba(58,53,48,.14)\n//   --shadow-frame:      0 8px 22px -6px rgba(58,53,48,.14), 0 2px 6px -2px rgba(58,53,48,.08)\n//   --shadow-cta-rest:   0 8px 18px -4px rgba(58,53,48,.28), 0 2px 6px -1px rgba(58,53,48,.16)\n//   --shadow-cta-hover:  0 14px 30px -6px rgba(58,53,48,.36), 0 4px 10px -2px rgba(58,53,48,.22)\n//   --shadow-accent-rest:  0 3px 9px -4px rgba(255,79,40,.28), 0 1px 3px -1px rgba(255,79,40,.16)\n//   --shadow-accent-hover: 0 14px 30px -6px rgba(255,79,40,.36), 0 4px 10px -2px rgba(255,79,40,.22)\n//   --shadow-state-success: 0 8px 22px -6px rgba(31,122,77,.40), 0 3px 8px -2px rgba(31,122,77,.24)\n//   --shadow-state-error:   0 8px 22px -6px rgba(255,79,40,.40), 0 3px 8px -2px rgba(255,79,40,.24)\n//   --glow-accent: 0 0 12px 0 rgba(255,79,40,.55), 0 0 4px 0 rgba(255,79,40,.40)\n// ============================================================\nconst STYLES = `\nhtml, body { margin: 0; padding: 0; }\n.lesson-root, .lesson-root * { box-sizing: border-box; }\n.lesson-root {\n  font-family: 'Manrope', system-ui, sans-serif;\n  color: ${T.ink};\n  background: ${T.bg};\n  height: 100dvh;\n  overflow: hidden;\n  -webkit-font-smoothing: antialiased;\n  font-feature-settings: \"ss01\",\"cv11\";\n}\n\n/* Reset дефолтных браузерных margin у заголовков и параграфов.\n   Без этого h1/h2/h3 имеют margin-top/bottom 0.67-0.83em (для шрифтов\n   45px это ~30-37px), которые сдвигают layout и могут визуально\n   скрывать прогресс-бар на экранах с большим заголовком вверху. */\n.lesson-root h1, .lesson-root h2, .lesson-root h3,\n.lesson-root h4, .lesson-root h5, .lesson-root h6,\n.lesson-root p, .lesson-root ul, .lesson-root ol {\n  margin: 0;\n}\n\n/* Типографика */\n.title { font-family: 'Source Serif 4', serif; font-weight: 600; line-height: 1.1; letter-spacing: -0.005em; font-variation-settings: \"opsz\" 60; }\n.display { font-family: 'Source Serif 4', serif; font-weight: 600; line-height: 1.0; letter-spacing: -0.01em; font-variation-settings: \"opsz\" 60; }\n.italic { font-family: 'Source Serif 4', serif; font-style: italic; font-weight: 500; font-variation-settings: \"opsz\" 60; }\n.mono { font-family: 'JetBrains Mono', monospace; }\n.mop { font-family: 'Manrope', sans-serif; font-weight: 600; color: ${T.ink}; display: inline-block; padding: 0 0.06em; }\n\n.frac { display: inline-flex; flex-direction: column; align-items: center; vertical-align: middle; line-height: 1; margin: 0 0.08em; font-family: 'Fraunces', serif; font-variation-settings: \"opsz\" 144; font-weight: 400; }\n.frac .n, .frac .d { padding: 0 0.12em; }\n.frac .bar { height: 0.08em; background: currentColor; width: 100%; margin: 0.08em 0; border-radius: 2px; }\n\n/* fade-up animation */\n@keyframes fade-in-up { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }\n.fade-up { animation: fade-in-up 0.4s ease-out forwards; opacity: 0; }\n.delay-1 { animation-delay: 0.12s; } .delay-2 { animation-delay: 0.24s; }\n.delay-3 { animation-delay: 0.36s; } .delay-4 { animation-delay: 0.48s; }\n\n/* feedback-block */\n.feedback-block { max-height: 0; opacity: 0; overflow: hidden; transition: max-height 0.4s ease-out, opacity 0.3s ease-out 0.1s, margin-top 0.4s ease-out; margin-top: 0; }\n.feedback-block.visible { max-height: 800px; opacity: 1; margin-top: clamp(16px, 2.5vw, 20px); }\n\n/* ============================================\n   КНОПКИ — НОВЫЙ ЯЗЫК v15\n   ============================================ */\n\n/* Dark CTA (резерв, для случаев где нужна именно тёмная кнопка) */\n.btn {\n  font-family: 'Manrope', sans-serif; font-weight: 600;\n  background: ${T.ink}; color: ${T.bg};\n  border: none; border-radius: 12px;\n  padding: clamp(12px, 2vw, 12px) clamp(18px, 2.5vw, 20px);\n  font-size: clamp(14px, 1.8vw, 14px);\n  cursor: pointer;\n  letter-spacing: 0.01em;\n  box-shadow:\n    0 8px 18px -4px rgba(${T.shadowBase}, 0.28),\n    0 2px 6px -1px rgba(${T.shadowBase}, 0.16);\n  transition: box-shadow 0.25s ease-out, background 0.2s;\n}\n.btn:hover:not(:disabled) {\n  background: ${T.accent};\n  box-shadow:\n    0 14px 30px -6px rgba(${T.shadowBase}, 0.36),\n    0 4px 10px -2px rgba(${T.shadowBase}, 0.22);\n}\n.btn:disabled { opacity: 0.4; cursor: not-allowed; }\n\n/* Главная CTA — белая с оранжевой тенью (Variant 7) */\n.btn-white-accent {\n  font-family: 'Manrope', sans-serif; font-weight: 600;\n  background: ${T.paper}; color: ${T.accent};\n  border: none; border-radius: 12px;\n  padding: clamp(12px, 2vw, 12px) clamp(24px, 3vw, 27px);\n  font-size: clamp(14px, 1.8vw, 14px);\n  cursor: pointer;\n  letter-spacing: 0.01em;\n  box-shadow:\n    0 3px 9px -4px rgba(255, 79, 40, 0.28),\n    0 1px 3px -1px rgba(255, 79, 40, 0.16);\n  transition: box-shadow 0.25s ease-out, background 0.2s, color 0.2s;\n}\n.btn-white-accent:hover:not(:disabled) {\n  background: ${T.accent};\n  color: ${T.paper};\n  box-shadow:\n    0 14px 30px -6px rgba(255, 79, 40, 0.36),\n    0 4px 10px -2px rgba(255, 79, 40, 0.22);\n}\n.btn-white-accent:disabled { opacity: 0.4; cursor: not-allowed; }\n\n/* Вторичная (Назад) — прозрачная в покое, белая карточка на hover (Variant 5) */\n.btn-ghost {\n  font-family: 'Manrope', sans-serif; font-weight: 600;\n  background: transparent; color: ${T.ink2};\n  border: none; border-radius: 12px;\n  padding: clamp(12px, 2vw, 12px) clamp(18px, 2.5vw, 20px);\n  font-size: clamp(14px, 1.8vw, 14px);\n  cursor: pointer;\n  box-shadow: 0 0 0 0 transparent;\n  transition: box-shadow 0.25s ease-out, background 0.2s, color 0.2s;\n}\n.btn-ghost:hover:not(:disabled) {\n  background: ${T.paper};\n  color: ${T.ink};\n  box-shadow:\n    0 8px 20px -5px rgba(${T.shadowBase}, 0.20),\n    0 2px 6px -2px rgba(${T.shadowBase}, 0.12);\n}\n.btn-ghost:disabled { opacity: 0.4; cursor: not-allowed; }\n\n/* ============================================\n   OPTION — без обводки, на тени\n   ============================================ */\n.option {\n  background: ${T.paper}; border: none;\n  border-radius: 12px;\n  font-family: 'Manrope', sans-serif; font-weight: 500;\n  text-align: left; width: 100%;\n  cursor: pointer;\n  box-shadow:\n    0 3px 9px -4px rgba(${T.shadowBase}, 0.16),\n    0 1px 3px -1px rgba(${T.shadowBase}, 0.10);\n  transition: box-shadow 0.25s ease-out, background 0.2s, color 0.2s;\n}\n.option:hover:not(:disabled):not(.option-correct):not(.option-picked-wrong):not(.option-wrong) {\n  background: #FDFBF7;\n  box-shadow:\n    0 10px 26px -6px rgba(${T.shadowBase}, 0.22),\n    0 3px 8px -2px rgba(${T.shadowBase}, 0.14);\n}\n.option:disabled { cursor: default; }\n\n.option-correct {\n  background: ${T.successSoft} !important;\n  color: ${T.success} !important;\n  box-shadow:\n    0 8px 22px -6px rgba(31, 122, 77, 0.40),\n    0 3px 8px -2px rgba(31, 122, 77, 0.24) !important;\n}\n.option-picked-wrong {\n  background: ${T.accentSoft} !important;\n  color: ${T.accent} !important;\n  box-shadow:\n    0 8px 22px -6px rgba(255, 79, 40, 0.40),\n    0 3px 8px -2px rgba(255, 79, 40, 0.24) !important;\n}\n.option-wrong {\n  background: ${T.paper} !important;\n  color: ${T.ink3} !important;\n  box-shadow: 0 2px 6px -3px rgba(${T.shadowBase}, 0.10) !important;\n  opacity: 0.55;\n}\n\n/* ============================================\n   ТИПОГРАФИЧЕСКИЕ РАЗМЕРЫ\n   ============================================ */\n.h-title { font-size: clamp(22px, 4vw, 38px); }\n.h-sub { font-size: clamp(17px, 2.5vw, 20px); }\n.body { font-size: clamp(15px, 1.9vw, 15px); line-height: 1.5; }\n.eyebrow { font-size: clamp(11px, 1.3vw, 11px); letter-spacing: 0.18em; text-transform: uppercase; font-weight: 600; }\n.small { font-size: clamp(13px, 1.5vw, 13px); }\n.frac-display { font-size: clamp(45px, 9vw, 75px); }\n.frac-mid { font-size: clamp(26px, 5vw, 38px); }\n.frac-sm { font-size: clamp(16px, 2.5vw, 20px); }\n\n/* ============================================\n   LAYOUT — 936px (infrastructure_v1)\n   ============================================ */\n.stage { max-width: 936px; margin: 0 auto; height: 100dvh; display: flex; flex-direction: column; }\n.stage-header {\n  flex-shrink: 0;\n  background: ${T.bg};\n  padding-top: clamp(12px, 2vw, 17px);\n  padding-bottom: clamp(8px, 1.5vw, 12px);\n  /* Горизонтальный padding через useIsMobile */\n}\n.stage-content {\n  flex: 1;\n  padding-top: clamp(8px, 1.5vw, 12px);\n  padding-bottom: clamp(20px, 4vw, 34px);\n  display: flex; flex-direction: column;\n  overflow-y: auto; overflow-x: hidden;\n  -webkit-overflow-scrolling: touch;\n}\n.stage-nav {\n  flex-shrink: 0;\n  background: ${T.bg};\n  border-top: 1px solid ${T.ink3}40;\n  padding-top: clamp(14px, 2.5vw, 15px);\n  padding-bottom: clamp(14px, 2.5vw, 15px);\n  /* Горизонтальный padding через useIsMobile */\n  display: flex; gap: 12px; align-items: center;\n}\n\n.chrome { display: flex; align-items: center; justify-content: space-between; margin-bottom: 0; }\n.chrome-left { display: flex; align-items: center; gap: 10px; color: ${T.ink2}; }\n\n/* Dot в chrome — с orange glow (в одном языке с прогресс-баром и slider handle) */\n.dot {\n  width: 7px; height: 7px; border-radius: 50%;\n  background: ${T.accent};\n  box-shadow:\n    0 0 6px 0 rgba(255, 79, 40, 0.7),\n    0 0 2px 0 rgba(255, 79, 40, 0.5);\n}\n\n/* Progress bar с glow вдоль оранжевой заливки.\n   Теперь stage-header вне скроллируемой области, overflow: visible\n   безопасен — glow свободно выходит за края трека. */\n.progress-track {\n  height: 3px;\n  background: ${T.ink3}40;\n  width: 100%;\n  margin-bottom: 14px;\n  border-radius: 99px;\n  overflow: visible;\n  position: relative;\n}\n.progress-bar {\n  height: 100%;\n  background: ${T.accent};\n  transition: width 0.5s cubic-bezier(0.4, 0, 0.2, 1);\n  border-radius: 99px;\n  box-shadow:\n    0 0 12px 0 rgba(255, 79, 40, 0.55),\n    0 0 4px 0 rgba(255, 79, 40, 0.40);\n}\n\n/* ============================================\n   SLIDER — новый формат с track-bg + track-fill\n   ============================================ */\n.track-wrap { position: relative; display: flex; align-items: center; margin: 20px 0; }\n.track-bg {\n  position: absolute; left: 0; right: 0; top: 50%; transform: translateY(-50%);\n  height: 4px;\n  background: ${T.ink3}40;\n  border-radius: 99px;\n  pointer-events: none;\n}\n.track-fill {\n  position: absolute; left: 0; top: 50%; transform: translateY(-50%);\n  height: 4px;\n  background: ${T.accent};\n  border-radius: 99px;\n  box-shadow:\n    0 0 12px 0 rgba(255, 79, 40, 0.55),\n    0 0 4px 0 rgba(255, 79, 40, 0.40);\n  pointer-events: none;\n  transition: width 0.15s ease-out;\n}\ninput[type=\"range\"].sl {\n  -webkit-appearance: none; appearance: none;\n  width: 100%; height: 4px;\n  background: transparent; outline: none;\n  position: relative; z-index: 2; margin: 0;\n}\ninput[type=\"range\"].sl::-webkit-slider-runnable-track { background: transparent; height: 4px; }\ninput[type=\"range\"].sl::-webkit-slider-thumb {\n  -webkit-appearance: none; appearance: none;\n  width: 24px; height: 24px;\n  background: ${T.accent};\n  border-radius: 50%;\n  border: 3px solid ${T.bg};\n  /* Равномерная круговая тень — offset-y = 0 */\n  box-shadow:\n    0 0 14px 0 rgba(255, 79, 40, 0.50),\n    0 0 5px 0 rgba(255, 79, 40, 0.32);\n  cursor: grab;\n  transition: transform 0.1s;\n  margin-top: -10px;\n}\ninput[type=\"range\"].sl::-webkit-slider-thumb:active { cursor: grabbing; transform: scale(1.12); }\ninput[type=\"range\"].sl::-moz-range-track { background: transparent; height: 4px; }\ninput[type=\"range\"].sl::-moz-range-thumb {\n  width: 24px; height: 24px;\n  background: ${T.accent};\n  border-radius: 50%;\n  border: 3px solid ${T.bg};\n  box-shadow:\n    0 0 14px 0 rgba(255, 79, 40, 0.50),\n    0 0 5px 0 rgba(255, 79, 40, 0.32);\n  cursor: grab;\n}\n\n/* ============================================\n   INPUT — без обводки, на тени; состояния на цветных тенях\n   ============================================ */\n.answer-input {\n  font-family: 'Fraunces', serif;\n  font-size: clamp(22px, 4vw, 27px);\n  font-weight: 400;\n  text-align: center;\n  border: none;\n  border-radius: 12px;\n  background: ${T.paper};\n  padding: 8px 12px;\n  outline: none;\n  box-shadow:\n    0 3px 9px -4px rgba(${T.shadowBase}, 0.16),\n    0 1px 3px -1px rgba(${T.shadowBase}, 0.10);\n  transition: box-shadow 0.25s ease-out, background 0.3s, color 0.3s;\n}\n.answer-input:focus {\n  box-shadow:\n    0 8px 20px -6px rgba(${T.shadowBase}, 0.22),\n    0 2px 5px -2px rgba(${T.shadowBase}, 0.14);\n}\n.answer-input.correct {\n  background: ${T.successSoft};\n  color: ${T.success};\n  box-shadow:\n    0 6px 16px -6px rgba(31, 122, 77, 0.36),\n    0 2px 5px -2px rgba(31, 122, 77, 0.20);\n}\n.answer-input.wrong {\n  background: ${T.accentSoft};\n  color: ${T.accent};\n  box-shadow:\n    0 6px 16px -6px rgba(255, 79, 40, 0.36),\n    0 2px 5px -2px rgba(255, 79, 40, 0.20);\n}\n\n/* ============================================\n   FRAMES — без обводки, на тени\n   Frame-soft / frame-success: polosa слева 4px СОХРАНЕНА\n   как намеренное исключение для функционального сигнала статуса\n   ============================================ */\n.frame {\n  background: ${T.paper};\n  border: none;\n  border-radius: 16px;\n  padding: clamp(20px, 4vw, 31px);\n  box-shadow:\n    0 8px 22px -6px rgba(${T.shadowBase}, 0.14),\n    0 2px 6px -2px rgba(${T.shadowBase}, 0.08);\n}\n.frame-soft {\n  background: ${T.accentSoft};\n  border: none;\n  border-left: 4px solid ${T.accent};\n  border-radius: 12px;\n  border-top-left-radius: 4px;\n  border-bottom-left-radius: 4px;\n  padding: clamp(16px, 3vw, 20px);\n  box-shadow:\n    0 6px 16px -6px rgba(255, 79, 40, 0.28),\n    0 2px 5px -2px rgba(255, 79, 40, 0.14);\n}\n.frame-success {\n  background: ${T.successSoft};\n  border: none;\n  border-left: 4px solid ${T.success};\n  border-radius: 12px;\n  border-top-left-radius: 4px;\n  border-bottom-left-radius: 4px;\n  padding: clamp(16px, 3vw, 20px);\n  box-shadow:\n    0 6px 16px -6px rgba(31, 122, 77, 0.28),\n    0 2px 5px -2px rgba(31, 122, 77, 0.14);\n}\n\n/* ============================================\n   PREVIEW: language switcher (только для методиста)\n   ============================================ */\n.lang-switch {\n  position: fixed;\n  top: 12px;\n  right: 12px;\n  z-index: 100;\n  display: flex;\n  gap: 4px;\n  background: ${T.paper};\n  border-radius: 99px;\n  padding: 3px;\n  box-shadow:\n    0 3px 9px -4px rgba(${T.shadowBase}, 0.16),\n    0 1px 3px -1px rgba(${T.shadowBase}, 0.10);\n}\n.lang-btn {\n  font-family: 'JetBrains Mono', monospace;\n  font-size: 12px;\n  font-weight: 600;\n  padding: 6px 14px;\n  border-radius: 99px;\n  border: none;\n  background: transparent;\n  color: ${T.ink2};\n  cursor: pointer;\n  transition: all 0.15s;\n  letter-spacing: 0.06em;\n}\n.lang-btn.active { background: ${T.ink}; color: ${T.bg}; }\n.lang-btn:hover:not(.active) { color: ${T.ink}; }\n`;\n```\n</content>\n</page>"}